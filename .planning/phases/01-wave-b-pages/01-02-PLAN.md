---
phase: 01-wave-b-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/costs/page.tsx
  - src/app/(dashboard)/system/page.tsx
autonomous: true
requirements:
  - WAVB-02
  - WAVB-03

must_haves:
  truths:
    - "Costs page chart containers use UUI tokens for layout and chrome — Recharts component internals (fill, stroke, Cell colors) remain exactly as-is"
    - "Costs page wrapper elements have zero var(--*) inline styles"
    - "System page renders with zero var(--*) inline styles including the hardcoded fallback pattern var(--info, #3b82f6)"
    - "System page inline logs modal uses UUI tokens exclusively"
  artifacts:
    - path: "src/app/(dashboard)/costs/page.tsx"
      provides: "Costs page with UUI tokens, Recharts internals preserved"
      contains: "cx("
    - path: "src/app/(dashboard)/system/page.tsx"
      provides: "System page with UUI tokens"
      contains: "cx("
  key_links:
    - from: "src/app/(dashboard)/costs/page.tsx"
      to: "@circos/ui"
      via: "import { cx } from '@circos/ui'"
      pattern: "import.*cx.*from.*@circos/ui"
    - from: "src/app/(dashboard)/system/page.tsx"
      to: "@circos/ui"
      via: "import { cx } from '@circos/ui'"
      pattern: "import.*cx.*from.*@circos/ui"
---

<objective>
Migrate the costs page and system page from legacy Digital Circus inline styles to UUI tokens and Tailwind utilities.

Purpose: Eliminate all var(--*) inline styles from these two Wave B pages. Costs requires careful allowlisting of Recharts internals. System is the largest file (613 lines) with the most style={{}} occurrences.
Output: Two fully migrated page files with zero legacy CSS variable usage.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/ui/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate costs/page.tsx to UUI tokens</name>
  <files>src/app/(dashboard)/costs/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/costs/page.tsx (current state — 388 lines, 73 style={{}} occurrences, 87 var(--*) tokens)
    - packages/ui/CLAUDE.md (component API reference and conventions)
  </read_first>
  <action>
Migrate all inline style={{}} patterns in costs/page.tsx to Tailwind UUI utility classes. This file has ~73 style={{}} and ~87 var(--*) occurrences. It uses Recharts (LineChart, BarChart, PieChart) with hardcoded COLORS array.

Token replacement rules (apply to ALL wrapper/container elements):
- `style={{ color: 'var(--text-primary)' }}` → className with `text-primary`
- `style={{ color: 'var(--text-secondary)' }}` → className with `text-secondary`
- `style={{ color: 'var(--text-muted)' }}` → className with `text-muted`
- `style={{ backgroundColor: 'var(--surface)' }}` → className with `bg-surface`
- `style={{ backgroundColor: 'var(--surface-elevated)' }}` → className with `bg-surface-elevated`
- `style={{ backgroundColor: 'var(--card)' }}` → className with `bg-card`
- `style={{ backgroundColor: 'var(--card-elevated)' }}` → className with `bg-card-elevated`
- `style={{ borderColor: 'var(--border)' }}` → className with `border-border`
- `style={{ borderColor: 'var(--border-strong)' }}` → className with `border-border-strong`
- `style={{ color: 'var(--accent)' }}` → `text-accent`
- `style={{ backgroundColor: 'var(--accent-soft)' }}` → `bg-accent-soft`
- `style={{ color: 'var(--positive)' }}` or `var(--success)` → `text-success`
- `style={{ backgroundColor: 'var(--success-bg)' }}` → `bg-success/10` or `bg-success-soft`
- `style={{ color: 'var(--warning)' }}` → `text-warning`
- `style={{ color: 'var(--negative)' }}` or `var(--error)` → `text-error`
- `style={{ color: 'var(--info)' }}` → `text-info`
- `style={{ fontFamily: 'var(--font-heading)' }}` → `font-heading`
- `style={{ fontFamily: 'var(--font-body)' }}` → `font-body`
- `style={{ fontFamily: 'var(--font-mono)' }}` → `font-mono`
- `style={{ backgroundColor: 'var(--background)' }}` → `bg-background`

CRITICAL ALLOWLIST — Do NOT change any of these Recharts internals:
- `<Line stroke="..." />` props
- `<Bar fill="..." />` props
- `<Pie fill="..." />` props
- `<Cell fill={COLORS[...]} />` props
- `<CartesianGrid stroke="..." />` props
- `<XAxis stroke="..." tick={{fill: ...}} />` props
- `<YAxis stroke="..." tick={{fill: ...}} />` props
- `<Tooltip contentStyle={{...}} />` props
- The `COLORS` constant array at the top of the file
- Any fill/stroke attribute on a Recharts component element

Only migrate the React container/wrapper `<div>`, `<h1>`, `<h2>`, `<p>`, `<span>`, `<button>` elements that WRAP the chart components. The Recharts `<LineChart>`, `<BarChart>`, `<RePieChart>`, `<ResponsiveContainer>` and their children stay exactly as-is.

Additional rules:
- Add `import { cx } from "@circos/ui"` at top of file
- Use cx() for conditional class merging
- Keep all existing Tailwind className values — only add/merge, never remove
  </action>
  <verify>
    <automated>grep "var(--" src/app/\(dashboard\)/costs/page.tsx | grep -v "Recharts\|Line \|Bar \|Pie \|Cell \|CartesianGrid\|XAxis\|YAxis\|Tooltip " | grep -c "var(--" | grep -q "^0$" && echo "PASS" || echo "FAIL: check non-Recharts var(-- usage"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "var(--" src/app/(dashboard)/costs/page.tsx` returns ONLY matches inside Recharts component props (stroke, fill, tick, contentStyle) or returns 0 matches total
    - `grep "cx(" src/app/(dashboard)/costs/page.tsx` returns at least 1 match
    - `grep "from.*@circos/ui" src/app/(dashboard)/costs/page.tsx` returns at least 1 match
    - File still contains `const COLORS =` array unchanged
    - File still imports from "recharts": LineChart, BarChart, PieChart (as RePieChart), ResponsiveContainer
    - `npx tsc --noEmit --pretty 2>&1 | grep -c "costs/page.tsx"` returns 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>costs/page.tsx wrapper/container elements have zero var(--*) inline styles, Recharts internals preserved exactly as-is, cx() from @circos/ui used for class merging</done>
</task>

<task type="auto">
  <name>Task 2: Migrate system/page.tsx to UUI tokens</name>
  <files>src/app/(dashboard)/system/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/system/page.tsx (current state — 613 lines, 111 style={{}} occurrences, 101 var(--*) tokens)
    - packages/ui/CLAUDE.md (component API reference and conventions)
  </read_first>
  <action>
Migrate all inline style={{}} patterns in system/page.tsx to Tailwind UUI utility classes. This is the heaviest file — 613 lines, ~111 style={{}} and ~101 var(--*) occurrences. It is fully self-contained with an inline logs modal.

Apply the same token replacement rules as Task 1 (see full mapping above).

Special attention:
- Replace the hardcoded fallback pattern `var(--info, #3b82f6)` with the `text-info` or `bg-info` Tailwind class (no fallback needed — UUI theme guarantees the token)
- The inline logs modal sub-component must be fully migrated — do not skip any style={{}} inside it
- If any plain `<button>` elements use manual styling and can be cleanly replaced with `<Button>` from @circos/ui, do so. Import Button alongside cx.
- If any `<input>` or `<select>` elements exist with manual styling, consider replacing with TextField/Select from @circos/ui

Additional rules:
- Add `import { cx } from "@circos/ui"` at top of file (add Button, Badge, etc. if replacing HTML elements)
- Use cx() for conditional class merging
- Keep all existing Tailwind className values — only add/merge, never remove
- For compound style={{}} with mixed token + non-token properties, convert tokens to Tailwind classes and keep only truly non-Tailwind-mappable properties in style={{}}
  </action>
  <verify>
    <automated>grep -c "var(--" src/app/\(dashboard\)/system/page.tsx | grep -q "^0$" && echo "PASS: zero var(-- tokens" || echo "FAIL: var(-- tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/app/(dashboard)/system/page.tsx` returns 0
    - `grep -c "var(--info, #3b82f6)" src/app/(dashboard)/system/page.tsx` returns 0 (hardcoded fallback removed)
    - `grep "cx(" src/app/(dashboard)/system/page.tsx` returns at least 1 match
    - `grep "from.*@circos/ui" src/app/(dashboard)/system/page.tsx` returns at least 1 match
    - `grep "style={{" src/app/(dashboard)/system/page.tsx` returns 0 OR only contains non-token CSS properties
    - `npx tsc --noEmit --pretty 2>&1 | grep -c "system/page.tsx"` returns 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>system/page.tsx contains zero var(--*) inline styles including the hardcoded fallback, uses cx() from @circos/ui, inline logs modal fully migrated, all interactive controls use UUI components where applicable</done>
</task>

</tasks>

<verification>
Run on both files after all tasks complete:

```bash
# Zero legacy tokens in both files
grep -c "var(--" src/app/\(dashboard\)/costs/page.tsx src/app/\(dashboard\)/system/page.tsx

# cx() imported and used
grep "cx(" src/app/\(dashboard\)/costs/page.tsx src/app/\(dashboard\)/system/page.tsx

# TypeScript compiles cleanly
npx tsc --noEmit --pretty 2>&1 | grep -E "(costs|system)/page.tsx" || echo "No TS errors"
```
</verification>

<success_criteria>
- costs/page.tsx: zero var(--*) tokens in wrapper elements, Recharts internals untouched, cx() from @circos/ui used
- system/page.tsx: zero var(--*) tokens including hardcoded fallback, cx() from @circos/ui used, inline logs modal migrated
- Both files compile with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-wave-b-pages/01-02-SUMMARY.md`
</output>
