---
phase: 01-wave-b-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/skills/page.tsx
  - src/app/(dashboard)/analytics/page.tsx
autonomous: true
requirements:
  - WAVB-01
  - WAVB-04

must_haves:
  truths:
    - "Skills page renders with zero var(--*) inline styles — all colors, typography, and spacing come from Tailwind UUI utilities"
    - "Skills inline sub-components (RegisterSkillModal, StatusBadge, SkillCard, SkillDetailModal) use UUI tokens exclusively"
    - "Analytics page chart wrappers use UUI tokens for layout — chart component internals remain unchanged"
    - "No style={{}} objects containing var(--*) remain in either file"
  artifacts:
    - path: "src/app/(dashboard)/skills/page.tsx"
      provides: "Skills page with UUI tokens"
      contains: "cx("
    - path: "src/app/(dashboard)/analytics/page.tsx"
      provides: "Analytics page with UUI tokens"
      contains: "cx("
  key_links:
    - from: "src/app/(dashboard)/skills/page.tsx"
      to: "@circos/ui"
      via: "import { cx } from '@circos/ui'"
      pattern: "import.*cx.*from.*@circos/ui"
    - from: "src/app/(dashboard)/analytics/page.tsx"
      to: "@circos/ui"
      via: "import { cx } from '@circos/ui'"
      pattern: "import.*cx.*from.*@circos/ui"
---

<objective>
Migrate the skills page and analytics page from legacy Digital Circus inline styles to UUI tokens and Tailwind utilities.

Purpose: Eliminate all var(--*) inline styles from these two Wave B pages, replacing them with Tailwind UUI utility classes and @circos/ui components where applicable.
Output: Two fully migrated page files with zero legacy CSS variable usage.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/ui/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate skills/page.tsx to UUI tokens</name>
  <files>src/app/(dashboard)/skills/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/skills/page.tsx (current state — 446 lines, 99 style={{}} occurrences, 72 var(--*) tokens)
    - packages/ui/CLAUDE.md (component API reference and conventions)
    - packages/ui/src/index.ts (available exports from @circos/ui)
  </read_first>
  <action>
Migrate all inline style={{}} patterns in skills/page.tsx to Tailwind UUI utility classes. This file has ~99 style={{}} and ~72 var(--*) occurrences across the main SkillsPage component and its 4 inline sub-components (RegisterSkillModal, StatusBadge, SkillCard, SkillDetailModal).

Token replacement rules (apply to ALL occurrences):
- `style={{ color: 'var(--text-primary)' }}` → className with `text-primary`
- `style={{ color: 'var(--text-secondary)' }}` → className with `text-secondary`
- `style={{ color: 'var(--text-muted)' }}` → className with `text-muted`
- `style={{ backgroundColor: 'var(--surface)' }}` → className with `bg-surface`
- `style={{ backgroundColor: 'var(--surface-elevated)' }}` → className with `bg-surface-elevated`
- `style={{ backgroundColor: 'var(--card)' }}` → className with `bg-card`
- `style={{ backgroundColor: 'var(--card-elevated)' }}` → className with `bg-card-elevated`
- `style={{ borderColor: 'var(--border)' }}` → className with `border-border`
- `style={{ borderColor: 'var(--border-strong)' }}` → className with `border-border-strong`
- `style={{ color: 'var(--accent)' }}` → className with `text-accent`
- `style={{ backgroundColor: 'var(--accent-soft)' }}` → className with `bg-accent-soft`
- `style={{ color: 'var(--positive)' }}` or `var(--success)` → `text-success`
- `style={{ backgroundColor: 'var(--success-bg)' }}` → `bg-success/10` or `bg-success-soft`
- `style={{ color: 'var(--warning)' }}` → `text-warning`
- `style={{ color: 'var(--negative)' }}` or `var(--error)` → `text-error`
- `style={{ backgroundColor: 'var(--error-bg)' }}` → `bg-error/10` or `bg-error-soft`
- `style={{ color: 'var(--info)' }}` → `text-info`
- `style={{ fontFamily: 'var(--font-heading)' }}` → `font-heading`
- `style={{ fontFamily: 'var(--font-body)' }}` → `font-body`
- `style={{ fontFamily: 'var(--font-mono)' }}` → `font-mono`
- `style={{ backgroundColor: 'var(--background)' }}` → `bg-background`
- `style={{ backgroundColor: 'var(--surface-hover)' }}` → `hover:bg-surface-hover`

For compound style={{}} objects with multiple properties, merge ALL into className using Tailwind utilities. When a style={{}} mixes var(--*) tokens with non-token properties (e.g., padding, gap), convert the token properties to Tailwind classes and keep only non-token properties in style={{}} if they have no Tailwind equivalent.

Additional rules:
- Add `import { cx } from "@circos/ui"` at top of file
- Use cx() for conditional class merging instead of string concatenation
- If any legacy HTML elements (plain `<button>`, `<input>`, `<select>`) use manual styling, consider replacing with @circos/ui components (Button, TextField, Select) — but only if the replacement is clean and does not change behavior
- Remove the `type CSSProperties` import if no longer needed after migration
- Do NOT touch the SmartAddModal import or usage — that component migrates in Phase 4
- Keep all existing className values that are already Tailwind utilities (e.g., "p-6") — only add/merge, never remove working classes
  </action>
  <verify>
    <automated>grep -c "var(--" src/app/\(dashboard\)/skills/page.tsx | grep -q "^0$" && echo "PASS: zero var(-- tokens" || echo "FAIL: var(-- tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/app/(dashboard)/skills/page.tsx` returns 0
    - `grep "cx(" src/app/(dashboard)/skills/page.tsx` returns at least 1 match
    - `grep "from.*@circos/ui" src/app/(dashboard)/skills/page.tsx` returns at least 1 match
    - `grep "style={{" src/app/(dashboard)/skills/page.tsx` returns 0 OR only contains non-token CSS properties (no var(--*) inside any remaining style={{}})
    - File still contains all 4 inline sub-component function declarations: RegisterSkillModal, StatusBadge, SkillCard, SkillDetailModal
    - File still imports SmartAddModal from '@/components/SmartAddModal'
    - `npx tsc --noEmit --pretty 2>&1 | grep -c "skills/page.tsx"` returns 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>skills/page.tsx contains zero var(--*) inline styles, uses cx() from @circos/ui for class merging, all 4 inline sub-components migrated, SmartAddModal import preserved unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Migrate analytics/page.tsx to UUI tokens</name>
  <files>src/app/(dashboard)/analytics/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/analytics/page.tsx (current state — 188 lines, 32 style={{}} occurrences, 32 var(--*) tokens)
    - packages/ui/CLAUDE.md (component API reference and conventions)
  </read_first>
  <action>
Migrate all inline style={{}} patterns in analytics/page.tsx to Tailwind UUI utility classes. This file has ~32 style={{}} and ~32 var(--*) occurrences. It is the lightest of the Wave B pages.

Apply the same token replacement rules as Task 1 (see full mapping above).

Additional rules:
- Add `import { cx } from "@circos/ui"` at top of file
- Use cx() for conditional class merging
- Do NOT modify the chart component imports (ActivityLineChart, ActivityPieChart, HourlyHeatmap, SuccessRateGauge) or their prop values — chart internals are allowlisted
- Only migrate the React wrapper/container JSX — the `<ActivityLineChart>`, `<ActivityPieChart>`, `<HourlyHeatmap>`, `<SuccessRateGauge>` component invocations and their props stay exactly as-is
- Merge existing Tailwind className values with new token classes using cx()
  </action>
  <verify>
    <automated>grep -c "var(--" src/app/\(dashboard\)/analytics/page.tsx | grep -q "^0$" && echo "PASS: zero var(-- tokens" || echo "FAIL: var(-- tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/app/(dashboard)/analytics/page.tsx` returns 0
    - `grep "cx(" src/app/(dashboard)/analytics/page.tsx` returns at least 1 match
    - `grep "from.*@circos/ui" src/app/(dashboard)/analytics/page.tsx` returns at least 1 match
    - File still imports all 4 chart components: ActivityLineChart, ActivityPieChart, HourlyHeatmap, SuccessRateGauge
    - `npx tsc --noEmit --pretty 2>&1 | grep -c "analytics/page.tsx"` returns 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>analytics/page.tsx contains zero var(--*) inline styles, uses cx() from @circos/ui, all chart component imports and props preserved unchanged</done>
</task>

</tasks>

<verification>
Run on both files after all tasks complete:

```bash
# Zero legacy tokens in both files
grep -c "var(--" src/app/\(dashboard\)/skills/page.tsx src/app/\(dashboard\)/analytics/page.tsx

# cx() imported and used
grep "cx(" src/app/\(dashboard\)/skills/page.tsx src/app/\(dashboard\)/analytics/page.tsx

# TypeScript compiles cleanly
npx tsc --noEmit --pretty 2>&1 | grep -E "(skills|analytics)/page.tsx" || echo "No TS errors"
```
</verification>

<success_criteria>
- skills/page.tsx: zero var(--*) tokens, cx() from @circos/ui used, all inline sub-components preserved
- analytics/page.tsx: zero var(--*) tokens, cx() from @circos/ui used, chart component imports unchanged
- Both files compile with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-wave-b-pages/01-01-SUMMARY.md`
</output>
