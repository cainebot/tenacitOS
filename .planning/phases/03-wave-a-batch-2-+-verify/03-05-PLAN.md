---
phase: 03-wave-a-batch-2-+-verify
plan: "05"
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified: []
autonomous: true
requirements: [VRFY-01, VRFY-02, VRFY-03]

must_haves:
  truths:
    - "Zero legacy var(--) tokens across ALL migrated files (shell + 16 pages)"
    - "next build succeeds with zero errors"
    - "Zero imports from src/components/ui/ legacy path in any migrated file"
  artifacts: []
  key_links: []
---

<objective>
Full verification sweep across ALL migrated files from phases 1-3 (shell + 16 pages). Confirm zero legacy tokens, clean build, and no legacy imports.

Purpose: Final gate to confirm the entire M6 migration is clean before marking complete.
Output: Verification results proving all three VRFY requirements pass.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wave-a-batch-2-+-verify/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify zero legacy tokens across all migrated files</name>
  <files></files>
  <read_first></read_first>
  <action>
Run grep across ALL migrated files to confirm zero legacy var(--) token references. The full list of migrated files:

PHASE 1 (App Shell):
- src/app/layout.tsx
- src/app/providers.tsx
- src/app/login/page.tsx
- src/app/(dashboard)/layout.tsx
- src/components/organisms/DashboardSidebar.tsx
- src/components/NodeStatusStrip.tsx
- src/components/atoms/BrandMark.tsx

PHASE 2 (Wave A Batch 1):
- src/app/(dashboard)/about/page.tsx
- src/app/(dashboard)/actions/page.tsx
- src/app/(dashboard)/activity/page.tsx
- src/app/(dashboard)/calendar/page.tsx
- src/app/(dashboard)/files/page.tsx
- src/app/(dashboard)/git/page.tsx
- src/app/(dashboard)/logs/page.tsx
- src/app/(dashboard)/memory/page.tsx

PHASE 3 (Wave A Batch 2):
- src/app/(dashboard)/organization/page.tsx
- src/app/(dashboard)/reports/page.tsx
- src/app/(dashboard)/search/page.tsx
- src/app/(dashboard)/sessions/page.tsx
- src/app/(dashboard)/settings/page.tsx
- src/app/(dashboard)/terminal/page.tsx
- src/app/(dashboard)/workflows/page.tsx
- src/app/(dashboard)/workspaces/page.tsx

For EACH file, run:
grep -n "var(--background)\|var(--surface)\|var(--card)\|var(--border)\b\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--font-heading)\|var(--font-body)\|var(--font-mono)\|var(--bg)\b" {file}

Expected result: 0 matches across all files.

ALSO check for hardcoded Tailwind color primitives that should have been migrated:
grep -n "text-white\|text-gray-\|bg-gray-\|border-gray-" across all migrated files.

If ANY legacy tokens are found, fix them immediately using the token mapping. If hardcoded Tailwind colors are found in non-allowlisted contexts, fix them too.

VRFY-01 passes when: grep returns 0 matches for legacy var(--) patterns across all 23 files.
  </action>
  <verify>
    <automated>grep -rl "var(--background)\|var(--surface)\|var(--card-\|var(--border)\b\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--font-heading)\|var(--font-body)\|var(--font-mono)" src/app/layout.tsx src/app/providers.tsx src/app/login/page.tsx "src/app/(dashboard)/layout.tsx" "src/app/(dashboard)/about/page.tsx" "src/app/(dashboard)/actions/page.tsx" "src/app/(dashboard)/activity/page.tsx" "src/app/(dashboard)/calendar/page.tsx" "src/app/(dashboard)/files/page.tsx" "src/app/(dashboard)/git/page.tsx" "src/app/(dashboard)/logs/page.tsx" "src/app/(dashboard)/memory/page.tsx" "src/app/(dashboard)/organization/page.tsx" "src/app/(dashboard)/reports/page.tsx" "src/app/(dashboard)/search/page.tsx" "src/app/(dashboard)/sessions/page.tsx" "src/app/(dashboard)/settings/page.tsx" "src/app/(dashboard)/terminal/page.tsx" "src/app/(dashboard)/workflows/page.tsx" "src/app/(dashboard)/workspaces/page.tsx" src/components/organisms/DashboardSidebar.tsx src/components/NodeStatusStrip.tsx src/components/atoms/BrandMark.tsx; echo "Exit code: $?"</automated>
  </verify>
  <acceptance_criteria>
- grep for legacy var(--) patterns across all 23 migrated files returns 0 matches
- Any found legacy tokens are fixed before marking complete
  </acceptance_criteria>
  <done>VRFY-01: Zero legacy var(--) tokens confirmed across all migrated files (shell + 16 pages)</done>
</task>

<task type="auto">
  <name>Task 2: Verify clean build and no legacy imports</name>
  <files></files>
  <read_first></read_first>
  <action>
Run two verification checks:

CHECK 1 — VRFY-02: Build verification
Run: npx next build
Expected: Build succeeds with zero errors. Warnings are acceptable.
If build fails, diagnose and fix the issue (likely a TypeScript error from migration).

CHECK 2 — VRFY-03: No legacy component imports
Run grep across all 23 migrated files for imports from the legacy path:
grep -rn "from ['\"]@/components/ui/" across all migrated files listed in Task 1.
Also check: grep -rn "from ['\"]../components/ui/" and grep -rn "from ['\"]../../components/ui/"

Expected: 0 matches. Imports should be from @openclaw/ui, not from src/components/ui/.

Note: imports from @/components/ (non-ui subdirectories like @/components/GlobalSearch, @/components/SystemInfo) are FINE — only src/components/ui/ is the legacy path.

If any legacy imports are found, replace with @openclaw/ui equivalents.

VRFY-02 passes when: next build exits 0.
VRFY-03 passes when: grep returns 0 matches for legacy ui/ imports.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- next build succeeds with exit code 0
- Zero imports from src/components/ui/ legacy path in any migrated file
  </acceptance_criteria>
  <done>VRFY-02: Build passes. VRFY-03: No legacy component imports found. Full M6 migration verified clean.</done>
</task>

</tasks>

<verification>
- All three VRFY requirements pass
- Zero legacy tokens (VRFY-01)
- Clean build (VRFY-02)
- No legacy imports (VRFY-03)
</verification>

<success_criteria>
All 23 migrated files have zero legacy var(--) tokens, zero imports from src/components/ui/, and the project builds successfully with next build.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-a-batch-2-+-verify/03-05-SUMMARY.md`
</output>
