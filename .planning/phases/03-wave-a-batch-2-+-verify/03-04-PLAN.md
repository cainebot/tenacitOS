---
phase: 03-wave-a-batch-2-+-verify
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/workflows/page.tsx
  - src/app/(dashboard)/workspaces/page.tsx
autonomous: true
requirements: [WAVE-15, WAVE-16]

must_haves:
  truths:
    - "Workflows page renders with zero legacy var(--) tokens"
    - "Workspaces page renders with zero legacy var(--) tokens"
    - "No imports from src/components/ui/ in either page"
  artifacts:
    - path: "src/app/(dashboard)/workflows/page.tsx"
      provides: "Workflows page with UUI tokens"
      contains: "bg-[var(--bg-secondary)]"
    - path: "src/app/(dashboard)/workspaces/page.tsx"
      provides: "Workspaces page with UUI tokens"
      contains: "bg-[var(--bg-secondary)]"
  key_links:
    - from: "src/app/(dashboard)/workflows/page.tsx"
      to: "UUI CSS custom properties"
      via: "Tailwind arbitrary value classes"
      pattern: "var\\(--bg-|var\\(--text-|var\\(--border-|var\\(--brand-"
---

<objective>
Migrate workflows (386 lines, 33 var refs, 25 style blocks) and workspaces (397 lines, 39 var refs, 46 style blocks) pages to 100% UUI tokens.

Purpose: Eliminate all legacy Digital Circus design tokens from these two medium pages.
Output: Two fully migrated page files with zero legacy var(--) references.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wave-a-batch-2-+-verify/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate workflows page to UUI tokens</name>
  <files>src/app/(dashboard)/workflows/page.tsx</files>
  <read_first>src/app/(dashboard)/workflows/page.tsx</read_first>
  <action>
Read the full workflows page (386 lines). Replace ALL legacy var(--) token references and style={} blocks with Tailwind arbitrary value classes using UUI CSS custom properties.

TOKEN MAPPING TABLE:
- var(--background), var(--bg) → bg-[var(--bg-primary)]
- var(--surface), var(--card) → bg-[var(--bg-secondary)]
- var(--surface-elevated), var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--border-strong) → border-[var(--border-secondary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--accent-soft) → bg-[var(--brand-600)]/10
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-[family-name:var(--font-display)]
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--font-mono) → font-[family-name:var(--font-code)]
- Semantic: success/positive → var(--success-600), error/negative → var(--error-600), warning → var(--warning-600), info → var(--blue-600)
- Semantic backgrounds: use /10 opacity
- text-white → text-[var(--text-primary-900)]
- text-gray-400 → text-[var(--text-quaternary-500)]
- bg-gray-700 → bg-[var(--bg-tertiary)]

RULES:
- Convert style={{}} props to className Tailwind classes wherever possible
- Replace JS hover handlers (onMouseEnter/onMouseLeave) with Tailwind hover: variants
- KEEP inline styles for: dynamic width/height percentages, conditional computed borders, data-driven color values
- Remove empty style={{}} props after migration
  </action>
  <verify>
    <automated>grep -c "var(--background)\|var(--surface)\|var(--card)\|var(--border)\b\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--font-heading)\|var(--font-body)\|var(--font-mono)" src/app/\(dashboard\)/workflows/page.tsx || echo "0 legacy tokens found"</automated>
  </verify>
  <acceptance_criteria>
- Zero legacy var(--) token references in workflows/page.tsx
- All style={{}} blocks with legacy tokens converted to Tailwind className
- Dynamic inline styles preserved where needed
  </acceptance_criteria>
  <done>Workflows page has zero legacy var(--) tokens; all semantic styles use UUI Tailwind arbitrary value classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate workspaces page to UUI tokens</name>
  <files>src/app/(dashboard)/workspaces/page.tsx</files>
  <read_first>src/app/(dashboard)/workspaces/page.tsx</read_first>
  <action>
Read the full workspaces page (397 lines). Replace ALL legacy var(--) token references and style={} blocks with Tailwind arbitrary value classes using UUI CSS custom properties. Apply the same token mapping as Task 1.

TOKEN MAPPING TABLE:
- var(--background), var(--bg) → bg-[var(--bg-primary)]
- var(--surface), var(--card) → bg-[var(--bg-secondary)]
- var(--surface-elevated), var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--border-strong) → border-[var(--border-secondary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--accent-soft) → bg-[var(--brand-600)]/10
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-[family-name:var(--font-display)]
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--font-mono) → font-[family-name:var(--font-code)]
- Semantic: success/positive → var(--success-600), error/negative → var(--error-600), warning → var(--warning-600), info → var(--blue-600)
- Semantic backgrounds: use /10 opacity
- text-white → text-[var(--text-primary-900)]
- text-gray-400 → text-[var(--text-quaternary-500)]
- bg-gray-700 → bg-[var(--bg-tertiary)]

RULES:
- Convert style={{}} props to className Tailwind classes wherever possible
- Replace JS hover handlers with Tailwind hover: variants
- KEEP inline styles for: dynamic width/height percentages, conditional computed borders
- Workspaces page uses useRealtimeStatus and has RAM bar color logic — dynamic getRamBarColor() return values are allowlisted as inline style since they are computed at runtime
- Remove empty style={{}} props after migration
  </action>
  <verify>
    <automated>grep -c "var(--background)\|var(--surface)\|var(--card)\|var(--border)\b\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--font-heading)\|var(--font-body)\|var(--font-mono)" src/app/\(dashboard\)/workspaces/page.tsx || echo "0 legacy tokens found"</automated>
  </verify>
  <acceptance_criteria>
- Zero legacy var(--) token references in workspaces/page.tsx
- All style={{}} blocks with legacy tokens converted to Tailwind className
- Dynamic RAM bar colors and width percentages preserved as inline styles
  </acceptance_criteria>
  <done>Workspaces page has zero legacy var(--) tokens; dynamic runtime styles preserved as allowlisted inline styles</done>
</task>

</tasks>

<verification>
- grep for legacy var(--) patterns in both files returns 0
- grep for imports from src/components/ui/ in both files returns 0
- No TypeScript errors in either file
</verification>

<success_criteria>
Both workflows and workspaces pages contain zero legacy var(--) token references and zero imports from the legacy src/components/ui/ path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-a-batch-2-+-verify/03-04-SUMMARY.md`
</output>
