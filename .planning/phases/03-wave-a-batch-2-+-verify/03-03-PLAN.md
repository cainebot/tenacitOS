---
phase: 03-wave-a-batch-2-+-verify
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/sessions/page.tsx
autonomous: true
requirements: [WAVE-12]

must_haves:
  truths:
    - "Sessions page renders with zero legacy var(--) tokens"
    - "No imports from src/components/ui/ in sessions page"
    - "Dynamic computed styles (percentages, conditional borders) preserved as inline styles"
  artifacts:
    - path: "src/app/(dashboard)/sessions/page.tsx"
      provides: "Sessions page with UUI tokens"
      contains: "bg-[var(--bg-secondary)]"
  key_links:
    - from: "src/app/(dashboard)/sessions/page.tsx"
      to: "UUI CSS custom properties"
      via: "Tailwind arbitrary value classes"
      pattern: "var\\(--bg-|var\\(--text-|var\\(--border-|var\\(--brand-"
---

<objective>
Migrate the sessions page (973 lines, 69 var refs, 85 style blocks) to 100% UUI tokens. This is the largest page in the batch — dedicated plan to stay within context budget.

Purpose: Sessions is the most complex page with nearly 1000 lines and 85 style blocks. Needs its own plan to ensure thorough migration without rushing.
Output: Fully migrated sessions page with zero legacy var(--) references.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wave-a-batch-2-+-verify/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sessions page to UUI tokens (lines 1-500)</name>
  <files>src/app/(dashboard)/sessions/page.tsx</files>
  <read_first>src/app/(dashboard)/sessions/page.tsx</read_first>
  <action>
Read the FULL sessions page (all 973 lines). This is the largest page in Wave A with 69 legacy var(--) references and 85 style={{}} blocks.

Replace ALL legacy var(--) token references with Tailwind arbitrary value classes. Work through the entire file systematically.

TOKEN MAPPING TABLE:
- var(--background), var(--bg) → bg-[var(--bg-primary)]
- var(--surface), var(--card) → bg-[var(--bg-secondary)]
- var(--surface-elevated), var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--border-strong) → border-[var(--border-secondary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--accent-soft) → bg-[var(--brand-600)]/10
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-[family-name:var(--font-display)]
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--font-mono) → font-[family-name:var(--font-code)]
- Semantic: success/positive → var(--success-600), error/negative → var(--error-600), warning → var(--warning-600), info → var(--blue-600)
- Semantic backgrounds: use /10 opacity (e.g. bg-[var(--success-600)]/10)
- text-white → text-[var(--text-primary-900)]
- text-gray-400 → text-[var(--text-quaternary-500)]
- bg-gray-700 → bg-[var(--bg-tertiary)]

RULES:
- Convert style={{}} props to className Tailwind classes wherever possible
- Replace JS hover handlers (onMouseEnter/onMouseLeave) with Tailwind hover: variants
- KEEP inline styles for: dynamic width/height percentages, conditional computed borders, data-driven color values
- Remove empty style={{}} props after migration
- If the page has type/status color maps using legacy tokens, convert them to UUI tokens in the data definitions (like Phase 2 did with typeClasses/statusClasses)
- Watch for rgba() overlays on modals — these are allowlisted as inline styles

Write the COMPLETE migrated file in one pass. Do not leave any legacy tokens.
  </action>
  <verify>
    <automated>grep -c "var(--background)\|var(--surface)\|var(--card)\|var(--border)\b\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--font-heading)\|var(--font-body)\|var(--font-mono)" src/app/\(dashboard\)/sessions/page.tsx || echo "0 legacy tokens found"</automated>
  </verify>
  <acceptance_criteria>
- Zero legacy var(--) token references in sessions/page.tsx
- All 85 style={{}} blocks with legacy tokens converted to Tailwind className
- Dynamic inline styles (percentages, computed values) preserved
- Type/status color maps updated to UUI semantic tokens
- No imports from src/components/ui/
  </acceptance_criteria>
  <done>Sessions page (973 lines) has zero legacy var(--) tokens; all semantic styles use UUI Tailwind arbitrary value classes; dynamic/computed styles preserved as inline</done>
</task>

</tasks>

<verification>
- grep for legacy var(--) patterns in sessions/page.tsx returns 0
- grep for imports from src/components/ui/ returns 0
- No TypeScript errors in the file
</verification>

<success_criteria>
Sessions page contains zero legacy var(--) token references, zero imports from legacy src/components/ui/ path, and all dynamic/computed styles are preserved as allowlisted inline styles.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-a-batch-2-+-verify/03-03-SUMMARY.md`
</output>
