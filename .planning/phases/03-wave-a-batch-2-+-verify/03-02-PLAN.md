---
phase: 03-wave-a-batch-2-+-verify
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/search/page.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/terminal/page.tsx
autonomous: true
requirements: [WAVE-11, WAVE-13, WAVE-14]

must_haves:
  truths:
    - "Search page renders with zero legacy var(--) tokens"
    - "Settings page renders with zero legacy var(--) tokens"
    - "Terminal page renders with zero legacy var(--) tokens (fixed terminal palette colors allowlisted)"
  artifacts:
    - path: "src/app/(dashboard)/search/page.tsx"
      provides: "Search page with UUI tokens"
      contains: "text-[var(--text-primary-900)]"
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Settings page with UUI tokens"
      contains: "bg-[var(--bg-secondary)]"
    - path: "src/app/(dashboard)/terminal/page.tsx"
      provides: "Terminal page with UUI tokens"
      contains: "bg-[var(--bg-secondary)]"
  key_links:
    - from: "src/app/(dashboard)/search/page.tsx"
      to: "UUI CSS custom properties"
      via: "Tailwind arbitrary value classes"
      pattern: "var\\(--text-primary-900\\)"
---

<objective>
Migrate search (23 lines, 2 var refs), settings (140 lines, 9 var refs, 7 style blocks), and terminal (273 lines, 6 var refs, 28 style blocks) pages to 100% UUI tokens.

Purpose: Three small-to-medium pages grouped together. Search is tiny (2 var refs). Settings is small. Terminal has many style blocks but most are likely fixed terminal-themed colors that should be allowlisted.
Output: Three fully migrated page files.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wave-a-batch-2-+-verify/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate search and settings pages to UUI tokens</name>
  <files>src/app/(dashboard)/search/page.tsx, src/app/(dashboard)/settings/page.tsx</files>
  <read_first>src/app/(dashboard)/search/page.tsx, src/app/(dashboard)/settings/page.tsx</read_first>
  <action>
SEARCH PAGE (23 lines, 2 var refs, 2 style blocks):
Read the full search page. Replace the 2 legacy var(--) references:
- style={{ color: "var(--text-primary)", fontFamily: "var(--font-heading)" }} → className with text-[var(--text-primary-900)] font-[family-name:var(--font-display)]
- style={{ color: "var(--text-secondary)" }} → className with text-[var(--text-secondary-700)]
Remove the now-empty style={{}} props.

SETTINGS PAGE (140 lines, 9 var refs, 7 style blocks):
Read the full settings page. Replace ALL legacy var(--) token references with Tailwind arbitrary value classes.

TOKEN MAPPING TABLE (apply to both files):
- var(--background), var(--bg) → bg-[var(--bg-primary)]
- var(--surface), var(--card) → bg-[var(--bg-secondary)]
- var(--surface-elevated), var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--border-strong) → border-[var(--border-secondary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--accent-soft) → bg-[var(--brand-600)]/10
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-[family-name:var(--font-display)]
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--font-mono) → font-[family-name:var(--font-code)]
- Semantic: success/positive → var(--success-600), error/negative → var(--error-600), warning → var(--warning-600), info → var(--blue-600)
- Semantic backgrounds: use /10 opacity
- text-white → text-[var(--text-primary-900)]
- text-gray-400 → text-[var(--text-quaternary-500)]
- bg-gray-700 → bg-[var(--bg-tertiary)]

RULES:
- Convert style={{}} props to className Tailwind classes wherever possible
- Replace JS hover handlers with Tailwind hover: variants
- KEEP inline styles for: dynamic width/height percentages, conditional computed borders
  </action>
  <verify>
    <automated>grep -c "var(--background)\|var(--surface)\|var(--card)\|var(--border)\b\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--font-heading)\|var(--font-body)\|var(--font-mono)" src/app/\(dashboard\)/search/page.tsx src/app/\(dashboard\)/settings/page.tsx || echo "0 legacy tokens found"</automated>
  </verify>
  <acceptance_criteria>
- Zero legacy var(--) token references in search/page.tsx
- Zero legacy var(--) token references in settings/page.tsx
- All style={{}} blocks with legacy tokens converted to Tailwind className
  </acceptance_criteria>
  <done>Search and settings pages have zero legacy var(--) tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate terminal page to UUI tokens</name>
  <files>src/app/(dashboard)/terminal/page.tsx</files>
  <read_first>src/app/(dashboard)/terminal/page.tsx</read_first>
  <action>
Read the full terminal page (273 lines, 6 var refs, 28 style blocks). This page has few legacy var refs but many style={{}} blocks. Most style blocks are likely terminal-themed fixed colors (dark backgrounds like #0d1117, #1a1a2e, green text like #00ff00, etc.) which are ALLOWLISTED — they are intentional terminal aesthetic, not legacy UUI tokens.

Replace the 6 legacy var(--) token references with Tailwind arbitrary value classes per the mapping.

TOKEN MAPPING TABLE:
- var(--background), var(--bg) → bg-[var(--bg-primary)]
- var(--surface), var(--card) → bg-[var(--bg-secondary)]
- var(--surface-elevated), var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--border-strong) → border-[var(--border-secondary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--accent-soft) → bg-[var(--brand-600)]/10
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-[family-name:var(--font-display)]
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--font-mono) → font-[family-name:var(--font-code)]
- Semantic: success/positive → var(--success-600), error/negative → var(--error-600), warning → var(--warning-600), info → var(--blue-600)
- text-white → text-[var(--text-primary-900)]
- text-gray-400 → text-[var(--text-quaternary-500)]
- bg-gray-700 → bg-[var(--bg-tertiary)]

ALLOWLISTED (do NOT change):
- Fixed terminal palette colors (hex values like #0d1117, #1a1a2e, #00ff00, #c9d1d9, #8b949e)
- Dynamic width/height percentages
- Conditional computed styles

RULES:
- Only convert the 6 legacy var(--) references to Tailwind classes
- Convert any non-terminal style={{}} blocks that use legacy tokens to Tailwind classes
- Leave all terminal-aesthetic fixed-color style blocks as inline styles
  </action>
  <verify>
    <automated>grep -c "var(--background)\|var(--surface)\|var(--card)\|var(--border)\b\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--font-heading)\|var(--font-body)\|var(--font-mono)" src/app/\(dashboard\)/terminal/page.tsx || echo "0 legacy tokens found"</automated>
  </verify>
  <acceptance_criteria>
- Zero legacy var(--) token references in terminal/page.tsx
- Terminal-themed fixed palette colors preserved as inline styles
- Non-terminal style blocks with legacy tokens converted to Tailwind className
  </acceptance_criteria>
  <done>Terminal page has zero legacy var(--) tokens; terminal-aesthetic fixed colors retained as allowlisted inline styles</done>
</task>

</tasks>

<verification>
- grep for legacy var(--) patterns in all three files returns 0
- grep for imports from src/components/ui/ in all three files returns 0
- Terminal page still has its fixed-color inline styles for terminal aesthetic
</verification>

<success_criteria>
Search, settings, and terminal pages contain zero legacy var(--) token references. Terminal page retains its intentional fixed terminal palette as inline styles.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-a-batch-2-+-verify/03-02-SUMMARY.md`
</output>
