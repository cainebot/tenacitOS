---
phase: 02-wave-a-batch-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/about/page.tsx
  - src/app/(dashboard)/actions/page.tsx
autonomous: true
requirements: [WAVE-01, WAVE-02]

must_haves:
  truths:
    - "About page renders with zero legacy tokens and zero style={} blocks (except dynamic values)"
    - "Actions page renders with zero legacy tokens and zero style={} blocks (except dynamic values)"
  artifacts:
    - path: "src/app/(dashboard)/about/page.tsx"
      provides: "About page with UUI tokens only"
      contains: "bg-[var(--bg-primary)]"
    - path: "src/app/(dashboard)/actions/page.tsx"
      provides: "Actions page with UUI tokens only"
      contains: "bg-[var(--bg-primary)]"
  key_links:
    - from: "src/app/(dashboard)/about/page.tsx"
      to: "@circos/ui tokens"
      via: "Tailwind arbitrary values"
      pattern: "var\\(--bg-primary\\)|var\\(--text-primary-900\\)"
    - from: "src/app/(dashboard)/actions/page.tsx"
      to: "@circos/ui tokens"
      via: "Tailwind arbitrary values"
      pattern: "var\\(--bg-primary\\)|var\\(--text-primary-900\\)"
---

<objective>
Migrate the about page (512 lines, 54 style blocks, 72 legacy vars) and actions page (374 lines, 38 style blocks, 29 legacy vars) to 100% UUI tokens.

Purpose: Eliminate all legacy Digital Circus tokens from these two pages so they use only @circos/ui semantic tokens via Tailwind arbitrary value syntax.
Output: Two page files with zero legacy var(--) references and zero style={} blocks (except allowlisted dynamic values).
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wave-a-batch-1/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate about page to UUI tokens</name>
  <files>src/app/(dashboard)/about/page.tsx</files>
  <read_first>src/app/(dashboard)/about/page.tsx</read_first>
  <action>
Migrate all 54 style={} blocks and 72 legacy var(--) references to Tailwind UUI token classes. This is the largest page in the batch (512 lines).

**Token mapping — apply throughout the file:**
- var(--background) / var(--bg) → bg-[var(--bg-primary)]
- var(--surface) / var(--card) → bg-[var(--bg-secondary)]
- var(--surface-elevated) / var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--border-strong) → border-[var(--border-secondary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--accent-soft) → bg-[var(--brand-600)]/10
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-[family-name:var(--font-display)]
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--font-mono) → font-[family-name:var(--font-code)]
- var(--success) / var(--positive) → var(--success-600), bg → bg-[var(--success-600)]/10
- var(--error) / var(--negative) → var(--error-600), bg → bg-[var(--error-600)]/10
- var(--warning) → var(--warning-600), bg → bg-[var(--warning-600)]/10
- var(--info) → var(--blue-600), bg → bg-[var(--blue-600)]/10

**Process for each style={} block:**
1. Read the CSS properties in the style object
2. Map each property to its Tailwind UUI equivalent class
3. Remove the style={} prop entirely
4. Add the equivalent Tailwind classes to className (merge with existing classes if present)
5. For dynamic values (e.g., width based on a variable/calculation), keep as inline style — these are allowlisted

**Do NOT:**
- Change any component logic, state, or data fetching
- Modify imports of shared components from @/components/ (those are out of scope)
- Remove "use client" directive
  </action>
  <verify>
    <automated>bash -c 'f="src/app/(dashboard)/about/page.tsx"; s=$(grep -c "style={" "$f" 2>/dev/null || echo 0); echo "style={} count: $s"; legacy=$(grep "var(--background)\|var(--surface)\|var(--card)\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--border)" "$f" | grep -v "var(--bg-\|var(--text-primary-\|var(--text-secondary-\|var(--border-primary\|var(--border-secondary\|var(--brand-" | wc -l); echo "legacy var refs: $legacy"; if [ "$legacy" -gt 0 ]; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
- grep -c "style={" src/app/(dashboard)/about/page.tsx returns 0 (or count of allowlisted dynamic values only)
- grep "var(--background)\|var(--surface)\|var(--card)\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--border)" src/app/(dashboard)/about/page.tsx | grep -v "var(--bg-\|var(--text-primary-\|var(--text-secondary-\|var(--border-primary\|var(--border-secondary\|var(--brand-" returns 0 lines
- File still compiles (no syntax errors)
  </acceptance_criteria>
  <done>About page has zero legacy var(--) tokens and zero non-dynamic style={} blocks. All styling uses Tailwind UUI arbitrary value classes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate actions page to UUI tokens</name>
  <files>src/app/(dashboard)/actions/page.tsx</files>
  <read_first>src/app/(dashboard)/actions/page.tsx</read_first>
  <action>
Migrate all 38 style={} blocks and 29 legacy var(--) references to Tailwind UUI token classes (374 lines).

**Token mapping — apply throughout the file:**
- var(--background) / var(--bg) → bg-[var(--bg-primary)]
- var(--surface) / var(--card) → bg-[var(--bg-secondary)]
- var(--surface-elevated) / var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--border-strong) → border-[var(--border-secondary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--accent-soft) → bg-[var(--brand-600)]/10
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-[family-name:var(--font-display)]
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--font-mono) → font-[family-name:var(--font-code)]
- var(--success) / var(--positive) → var(--success-600), bg → bg-[var(--success-600)]/10
- var(--error) / var(--negative) → var(--error-600), bg → bg-[var(--error-600)]/10
- var(--warning) → var(--warning-600), bg → bg-[var(--warning-600)]/10
- var(--info) → var(--blue-600), bg → bg-[var(--blue-600)]/10

**Process for each style={} block:**
1. Read the CSS properties in the style object
2. Map each property to its Tailwind UUI equivalent class
3. Remove the style={} prop entirely
4. Add the equivalent Tailwind classes to className (merge with existing classes if present)
5. For dynamic values (e.g., width based on a variable/calculation), keep as inline style — these are allowlisted

**Do NOT:**
- Change any component logic, state, or data fetching
- Modify imports of shared components from @/components/
- Remove "use client" directive
  </action>
  <verify>
    <automated>bash -c 'f="src/app/(dashboard)/actions/page.tsx"; s=$(grep -c "style={" "$f" 2>/dev/null || echo 0); echo "style={} count: $s"; legacy=$(grep "var(--background)\|var(--surface)\|var(--card)\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--border)" "$f" | grep -v "var(--bg-\|var(--text-primary-\|var(--text-secondary-\|var(--border-primary\|var(--border-secondary\|var(--brand-" | wc -l); echo "legacy var refs: $legacy"; if [ "$legacy" -gt 0 ]; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
- grep -c "style={" src/app/(dashboard)/actions/page.tsx returns 0 (or count of allowlisted dynamic values only)
- grep "var(--background)\|var(--surface)\|var(--card)\|var(--accent)\|var(--text-primary)\|var(--text-secondary)\|var(--text-muted)\|var(--border)" src/app/(dashboard)/actions/page.tsx | grep -v "var(--bg-\|var(--text-primary-\|var(--text-secondary-\|var(--border-primary\|var(--border-secondary\|var(--brand-" returns 0 lines
- File still compiles (no syntax errors)
  </acceptance_criteria>
  <done>Actions page has zero legacy var(--) tokens and zero non-dynamic style={} blocks. All styling uses Tailwind UUI arbitrary value classes.</done>
</task>

</tasks>

<verification>
Both pages have zero legacy var(--) references (excluding UUI semantic tokens) and zero non-dynamic style={} blocks. Files compile without errors.
</verification>

<success_criteria>
- About page: 0 legacy vars, 0 non-dynamic style={}
- Actions page: 0 legacy vars, 0 non-dynamic style={}
- Both files unchanged in logic/state/data-fetching behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-wave-a-batch-1/02-01-SUMMARY.md`
</output>
