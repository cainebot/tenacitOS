---
phase: 01-app-shell
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/organisms/DashboardSidebar.tsx
  - src/components/NodeStatusStrip.tsx
autonomous: true
requirements: [SHELL-04, SHELL-05]

must_haves:
  truths:
    - "DashboardSidebar renders navigation with zero inline style attributes and zero legacy var(--*) tokens"
    - "NodeStatusStrip renders node cards, skeleton cards, and notifications with zero inline style attributes and zero legacy var(--*) tokens"
    - "Sidebar collapse/expand still works (width toggle between 56px and 240px)"
    - "Node status colors (success/warning/error) use UUI semantic tokens"
  artifacts:
    - path: "src/components/organisms/DashboardSidebar.tsx"
      provides: "Sidebar navigation with UUI tokens and Tailwind classes"
      min_lines: 100
    - path: "src/components/NodeStatusStrip.tsx"
      provides: "Node status strip with UUI tokens and Tailwind classes"
      min_lines: 100
  key_links:
    - from: "src/components/organisms/DashboardSidebar.tsx"
      to: "src/components/atoms/BrandMark.tsx"
      via: "imports BrandMark"
      pattern: "import.*BrandMark"
    - from: "src/components/organisms/DashboardSidebar.tsx"
      to: "src/components/NodeStatusStrip.tsx"
      via: "renders NodeStatusStrip in sidebar footer"
      pattern: "<NodeStatusStrip"
---

<objective>
Migrate DashboardSidebar and NodeStatusStrip from inline styles with legacy tokens to Tailwind + UUI semantic tokens.

Purpose: These are the largest inline-style-heavy components in the app shell. DashboardSidebar is the main navigation surface; NodeStatusStrip shows live node health in the sidebar footer.
Output: Both components fully migrated to Tailwind classes with UUI tokens, zero inline styles, zero legacy var(--*).
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Token mapping reference for this plan -->

Legacy → UUI replacement:
- var(--surface) → bg-[var(--bg-secondary)]
- var(--surface-hover) → bg-[var(--bg-quaternary)]
- var(--border) → border-[var(--border-primary)]
- var(--accent) → text-[var(--brand-600)]
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-display
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--card) → bg-[var(--bg-secondary)]
- var(--success) → var(--success-600)
- var(--warning) → var(--warning-600)
- var(--error) → var(--error-600)

<!-- cx() is already imported in DashboardSidebar from @circos/ui -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DashboardSidebar to Tailwind + UUI tokens</name>
  <files>src/components/organisms/DashboardSidebar.tsx</files>
  <read_first>
    - src/components/organisms/DashboardSidebar.tsx (310 lines, massive inline style usage)
    - packages/ui/src/components/application/app-navigation.tsx (reference for UUI nav patterns — note: we are NOT replacing with AppNavigation component directly since the sidebar has sections, collapse logic, bottom nav, and NodeStatusStrip that AppNavigation does not support; instead we adopt the UUI styling patterns)
  </read_first>
  <action>
Convert ALL inline `style={{...}}` attributes to Tailwind utility classes with UUI tokens. The file must end with ZERO `style={{` attributes and ZERO `style={collapsed ?` ternaries. Also remove the `onMouseEnter`/`onMouseLeave` JS hover handlers and use Tailwind `hover:` instead.

**Specific replacements:**

1. `<aside>` element — replace entire style={{...}} with className:
   - Width: Use conditional `cx(collapsed ? "w-14 min-w-14" : "w-60 min-w-60")`
   - Position/height: `sticky top-0 h-screen`
   - Layout: `flex flex-col`
   - Background: `bg-[var(--bg-secondary)]`
   - Border: `border-r border-[var(--border-primary)]`
   - Transition: `transition-[width,min-width] duration-200 ease-in-out`
   - Overflow: `overflow-hidden`

2. Collapsed "DC" header div (line ~177-179):
   - `style={{ height: '48px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}`
   - Replace with: `className="flex h-12 items-center justify-center"`
   - Inner span `style={{ fontSize: '18px', fontWeight: 700, color: 'var(--accent)', fontFamily: 'var(--font-heading)' }}`
   - Replace with: `className="text-lg font-bold text-[var(--brand-600)] font-display"`

3. Divider div (line ~183):
   - `style={{ height: '1px', backgroundColor: 'var(--border)', margin: collapsed ? '0 8px' : '0 12px' }}`
   - Replace with: `className={cx("h-px bg-[var(--border-primary)]", collapsed ? "mx-2" : "mx-3")}`

4. `<nav>` element (line ~188-191):
   - Remove `style={{ minHeight: 0, padding: collapsed ? '12px 6px' : '16px 12px' }}`
   - Update className: `cx("flex-1 flex flex-col gap-4 overflow-y-auto min-h-0", collapsed ? "p-3 px-1.5" : "p-4 px-3")`

5. Section label div (line ~198-206):
   - Remove entire `style={{...}}`
   - Replace with: `className="text-[10px] font-semibold tracking-[0.08em] text-[var(--text-quaternary-500)] pl-2 pb-1.5 font-display"`

6. Link items — collapsed ternary style (lines ~223-228, ~252-257):
   - Remove `style={collapsed ? { display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '8px 0' } : undefined}`
   - Add conditional classes: update the cx() call to include `cx("nav-item", active && "active", collapsed && "flex items-center justify-center py-2")`

7. Bottom nav container div (line ~241):
   - `style={{ borderTop: '1px solid var(--border)', padding: collapsed ? '8px 6px 4px' : '8px 12px 4px' }}`
   - Replace with: `className={cx("border-t border-[var(--border-primary)]", collapsed ? "px-1.5 pt-2 pb-1" : "px-3 pt-2 pb-1")}`

8. Collapse toggle container (line ~268):
   - `style={{ borderTop: '1px solid var(--border)', padding: '4px 6px' }}`
   - Replace with: `className="border-t border-[var(--border-primary)] p-1 px-1.5"`

9. Collapse toggle button (lines ~270-295):
   - Remove the entire `style={{...}}` object (display, alignItems, justifyContent, gap, width, padding, background, border, cursor, color, fontSize, fontFamily, borderRadius, transition)
   - Remove `onMouseEnter` and `onMouseLeave` handlers completely
   - Replace with className: `cx("flex items-center gap-2 w-full rounded-md text-xs text-[var(--text-quaternary-500)] font-[family-name:var(--font-text)] cursor-pointer bg-transparent border-none hover:bg-[var(--bg-quaternary)] hover:text-[var(--text-primary-900)] transition-colors", collapsed ? "justify-center py-2" : "justify-start py-2 px-2")`

10. NodeStatusStrip container (line ~304):
    - `style={{ borderTop: '1px solid var(--border)' }}`
    - Replace with: `className="px-3 py-3 border-t border-[var(--border-primary)]"` (merge with existing className)

Keep all imports, NAV_SECTIONS data, BOTTOM_NAV data, NavItem/NavSection interfaces, component logic, and Link routing unchanged.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-c && grep -c "style={{" src/components/organisms/DashboardSidebar.tsx | grep "^0$" && grep -c "style={collapsed" src/components/organisms/DashboardSidebar.tsx | grep "^0$" && grep -c "onMouseEnter" src/components/organisms/DashboardSidebar.tsx | grep "^0$" && grep -c "var(--surface)" src/components/organisms/DashboardSidebar.tsx | grep "^0$" && grep -c "var(--accent)" src/components/organisms/DashboardSidebar.tsx | grep "^0$" && grep -c "var(--font-heading)" src/components/organisms/DashboardSidebar.tsx | grep "^0$" && grep "bg-\[var(--bg-secondary)\]" src/components/organisms/DashboardSidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/organisms/DashboardSidebar.tsx contains ZERO `style={{` attributes
    - src/components/organisms/DashboardSidebar.tsx contains ZERO `style={collapsed` ternaries
    - src/components/organisms/DashboardSidebar.tsx contains ZERO `onMouseEnter` handlers
    - src/components/organisms/DashboardSidebar.tsx contains ZERO `onMouseLeave` handlers
    - src/components/organisms/DashboardSidebar.tsx does NOT contain `var(--surface)`, `var(--accent)`, `var(--font-heading)`, `var(--font-body)`, `var(--text-muted)`, `var(--text-primary)` (without -900), `var(--text-secondary)` (without -700), `var(--border)` (without -primary/-secondary suffix)
    - src/components/organisms/DashboardSidebar.tsx contains `bg-[var(--bg-secondary)]` (sidebar background)
    - src/components/organisms/DashboardSidebar.tsx contains `border-[var(--border-primary)]`
    - src/components/organisms/DashboardSidebar.tsx contains `text-[var(--brand-600)]` (collapsed DC text)
    - src/components/organisms/DashboardSidebar.tsx contains `font-display` (section labels, DC text)
    - src/components/organisms/DashboardSidebar.tsx contains `hover:bg-[var(--bg-quaternary)]` (collapse button hover)
    - src/components/organisms/DashboardSidebar.tsx still contains `import { cx } from "@circos/ui"`
    - src/components/organisms/DashboardSidebar.tsx still contains `NAV_SECTIONS` array
    - src/components/organisms/DashboardSidebar.tsx still contains `collapsed` state
  </acceptance_criteria>
  <done>DashboardSidebar uses only Tailwind + UUI tokens. Zero inline styles. Hover effects via Tailwind hover: prefix. Collapse/expand logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate NodeStatusStrip to Tailwind + UUI tokens</name>
  <files>src/components/NodeStatusStrip.tsx</files>
  <read_first>
    - src/components/NodeStatusStrip.tsx (340 lines, extensive inline styles in NodeCard, SkeletonCard, notifications, and status indicators)
  </read_first>
  <action>
Convert ALL inline `style={{...}}` attributes in NodeStatusStrip.tsx to Tailwind utility classes with UUI tokens. The file must end with ZERO `style={{` attributes. Import cx from @circos/ui at the top.

Add import: `import { cx } from "@circos/ui"`

**Helper functions — update return values to use UUI tokens:**

1. `getRamBarColor(percent)`: Return Tailwind class strings instead of CSS values:
   - `> 85` → return `"bg-[var(--error-600)]"`
   - `> 70` → return `"bg-[var(--warning-600)]"`
   - default → return `"bg-[var(--success-600)]"`

2. `statusDotColor(status)`: Return Tailwind class strings:
   - `online` → return `"bg-[var(--success-600)]"`
   - `degraded` → return `"bg-[var(--warning-600)]"`
   - default → return `"bg-[var(--error-600)]"`

3. `StatusIcon({ status })`: Replace inline styles with className:
   - online: `className="text-[10px] font-bold text-[var(--success-600)]"`
   - degraded: `className="text-[10px] font-bold text-[var(--warning-600)]"`
   - offline: `className="text-[10px] font-bold text-[var(--error-600)]"`

**NodeCard component — replace ALL style={{}} with className:**

4. Outer div: `className="flex flex-col gap-1 px-2.5 py-1.5 bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-md min-w-[120px] shrink-0"`

5. Node ID row: `className="flex items-center gap-1.5"`

6. Status dot span: `className={cx("inline-block w-2 h-2 rounded-full shrink-0", statusDotColor(node.status))}"`
   (Uses the updated statusDotColor that returns a Tailwind class)

7. Node ID text span: `className="text-[11px] font-semibold text-[var(--text-primary-900)] overflow-hidden text-ellipsis whitespace-nowrap"`

8. RAM bar outer div: `className="w-full h-[3px] bg-[var(--border-primary)] rounded-sm overflow-hidden"`

9. RAM bar inner div: `className={cx("h-full rounded-sm transition-[width] duration-[400ms] ease-in-out", getRamBarColor(ramPercent))}"`
   - Keep `style={{ width: \`${ramPercent}%\` }}` — this is the ONLY acceptable inline style (dynamic percentage)

10. Agent count row: `className="flex justify-between items-center"`

11. RAM percent text: `className="text-[10px] text-[var(--text-secondary-700)]"`

12. Agent count badge: `className="text-[10px] bg-[var(--border-primary)] rounded px-1.5 py-px text-[var(--text-primary-900)]"`

**SkeletonCard component:**

13. Outer div: `className="flex flex-col gap-1.5 px-2.5 py-1.5 bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-md min-w-[120px] shrink-0"`

14. Skeleton bars: Replace inline styles with className. Use `className="h-3 w-4/5 bg-white/[0.08] rounded animate-pulse"`, `className="h-[3px] w-full bg-white/[0.08] rounded-sm animate-pulse"`, `className="h-2.5 w-3/5 bg-white/[0.08] rounded animate-pulse"`

**NodeStatusStrip component:**

15. Outer div: `className="flex flex-col gap-1"`

16. Disconnection banner: `className="bg-[var(--warning-600)]/15 border border-[var(--warning-600)] rounded px-2.5 py-1 text-[11px] text-[var(--warning-600)] text-center"`

17. Node cards strip: `className="flex gap-2 overflow-x-auto pb-0.5"`

18. "No nodes registered" span: `className="text-[11px] text-[var(--text-secondary-700)] py-1.5"`

19. Notifications container: `className="flex flex-col gap-1 mt-1"`

20. Individual notification div: Replace with className using cx():
    `className={cx("flex items-center justify-between gap-2 px-2.5 py-1.5 rounded text-[11px] font-medium animate-[fadeIn_0.2s_ease]", notif.type === "offline" ? "bg-[var(--error-600)]/[0.12] border border-[var(--error-600)] text-[var(--error-600)]" : "bg-[var(--success-600)]/[0.12] border border-[var(--success-600)] text-[var(--success-600)]")}`

21. Dismiss button: `className="bg-transparent border-none cursor-pointer text-inherit text-xs leading-none px-0.5 opacity-70 shrink-0"`

The ONLY remaining inline style should be the RAM bar width percentage: `style={{ width: \`${ramPercent}%\` }}` — this is dynamic and cannot be a Tailwind class.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-c && STYLE_COUNT=$(grep -c "style={{" src/components/NodeStatusStrip.tsx); test "$STYLE_COUNT" -le 1 && echo "OK: $STYLE_COUNT inline styles (expected 0 or 1 for RAM bar)" && grep -c "var(--card)" src/components/NodeStatusStrip.tsx | grep "^0$" && grep -c "var(--border," src/components/NodeStatusStrip.tsx | grep "^0$" && grep -c "var(--text-primary," src/components/NodeStatusStrip.tsx | grep "^0$" && grep "import.*cx.*@circos/ui" src/components/NodeStatusStrip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NodeStatusStrip.tsx contains at most 1 `style={{` (the RAM bar dynamic width)
    - src/components/NodeStatusStrip.tsx does NOT contain `var(--card)`, `var(--border,` (with comma fallback), `var(--text-primary,` (with comma fallback), `var(--text-secondary,` (with comma fallback), `var(--success,` (with comma fallback), `var(--warning,` (with comma fallback), `var(--error,` (with comma fallback)
    - src/components/NodeStatusStrip.tsx contains `import { cx } from "@circos/ui"`
    - src/components/NodeStatusStrip.tsx contains `bg-[var(--bg-secondary)]` (card backgrounds)
    - src/components/NodeStatusStrip.tsx contains `border-[var(--border-primary)]`
    - src/components/NodeStatusStrip.tsx contains `text-[var(--success-600)]`
    - src/components/NodeStatusStrip.tsx contains `text-[var(--error-600)]`
    - src/components/NodeStatusStrip.tsx contains `text-[var(--warning-600)]`
    - src/components/NodeStatusStrip.tsx contains `text-[var(--text-primary-900)]`
    - src/components/NodeStatusStrip.tsx contains `animate-pulse` (skeleton cards)
    - src/components/NodeStatusStrip.tsx still contains `useRealtimeStatus` import
    - src/components/NodeStatusStrip.tsx still contains `NodeCard` component
  </acceptance_criteria>
  <done>NodeStatusStrip uses only Tailwind + UUI tokens. At most 1 inline style for dynamic RAM bar width. All status colors use UUI semantic tokens. Zero legacy var(--*) tokens with fallback values.</done>
</task>

</tasks>

<verification>
- `grep -c "style={{" src/components/organisms/DashboardSidebar.tsx` returns 0
- `grep -c "style={{" src/components/NodeStatusStrip.tsx` returns 0 or 1 (only RAM bar width)
- `grep -rE "var\(--(surface|accent|font-heading|font-body|text-muted|card)\b" src/components/organisms/DashboardSidebar.tsx src/components/NodeStatusStrip.tsx` returns no matches
- `npx next build 2>&1 | tail -5` completes without errors
</verification>

<success_criteria>
- DashboardSidebar has zero inline styles and uses only UUI tokens
- NodeStatusStrip has at most 1 inline style (RAM bar width) and uses only UUI tokens
- Both components import cx from @circos/ui
- Collapse/expand behavior preserved in sidebar
- Status colors use UUI semantic tokens (success-600, warning-600, error-600)
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell/01-03-SUMMARY.md`
</output>
