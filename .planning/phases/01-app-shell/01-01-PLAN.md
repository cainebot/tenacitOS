---
phase: 01-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/providers.tsx
  - src/app/layout.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02]

must_haves:
  truths:
    - "Every page is wrapped in ThemeProvider and UUIRouterProvider from @circos/ui"
    - "Dashboard layout renders with zero legacy var(--*) tokens"
    - "Root layout body uses UUI semantic tokens instead of inline var(--background)/var(--foreground)/var(--font-body)"
  artifacts:
    - path: "src/app/providers.tsx"
      provides: "Client wrapper combining ThemeProvider + UUIRouterProvider"
      contains: "ThemeProvider"
      exports: ["Providers"]
    - path: "src/app/layout.tsx"
      provides: "Root layout wrapping children in Providers"
      contains: "Providers"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with UUI tokens"
      contains: "bg-[var(--bg-primary)]"
  key_links:
    - from: "src/app/providers.tsx"
      to: "@circos/ui"
      via: "import { ThemeProvider, UUIRouterProvider }"
      pattern: "import.*ThemeProvider.*@circos/ui"
    - from: "src/app/layout.tsx"
      to: "src/app/providers.tsx"
      via: "wraps children in <Providers>"
      pattern: "<Providers>"
---

<objective>
Create the client Providers wrapper and migrate root + dashboard layouts to UUI tokens.

Purpose: Establishes the ThemeProvider and UUIRouterProvider foundation that every page inherits — prerequisite for all other migration work.
Output: providers.tsx client wrapper, migrated root layout, migrated dashboard layout.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- UUI providers the executor must import -->

From packages/ui/src/providers/theme.tsx:
```typescript
export function ThemeProvider({ children }: { children: ReactNode }): JSX.Element
// Uses next-themes with attribute="data-theme" defaultTheme="dark"
```

From packages/ui/src/providers/router-provider.tsx:
```typescript
export function UUIRouterProvider({ children }: { children: ReactNode }): JSX.Element
// Uses React Aria RouterProvider with next/navigation router.push
```

From packages/ui/src/index.ts:
```typescript
export * from "./providers"  // ThemeProvider, UUIRouterProvider
export { cx } from "./utils/cx"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Providers wrapper and migrate root layout</name>
  <files>src/app/providers.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current root layout — server component with inline styles)
    - packages/ui/src/providers/theme.tsx (ThemeProvider signature)
    - packages/ui/src/providers/router-provider.tsx (UUIRouterProvider signature)
  </read_first>
  <action>
1. Create src/app/providers.tsx — a "use client" component that wraps children in ThemeProvider then UUIRouterProvider:
```typescript
"use client"
import type { ReactNode } from "react"
import { ThemeProvider, UUIRouterProvider } from "@circos/ui"

export function Providers({ children }: { children: ReactNode }) {
  return (
    <ThemeProvider>
      <UUIRouterProvider>
        {children}
      </UUIRouterProvider>
    </ThemeProvider>
  )
}
```

2. Modify src/app/layout.tsx:
   - Keep it as a server component (NO "use client" directive)
   - Import Providers from "./providers"
   - Remove the `className="dark"` from `<html>` (ThemeProvider handles this via data-theme attribute)
   - Wrap {children} inside `<Providers>{children}</Providers>` inside the `<body>` tag
   - Remove the inline style={{ backgroundColor: 'var(--background)', color: 'var(--foreground)', fontFamily: 'var(--font-body)' }} from `<body>`
   - Replace with Tailwind classes on body: `bg-[var(--bg-primary)] text-[var(--text-primary-900)] font-[family-name:var(--font-text)]`
   - Keep all existing font variable classes (inter.variable, sora.variable, jetbrainsMono.variable) and font-sans
   - Keep the metadata export, manifest, themeColor, appleWebApp, icons unchanged
   - Keep the service worker script unchanged
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-c && grep -c "ThemeProvider" src/app/providers.tsx && grep -c "UUIRouterProvider" src/app/providers.tsx && grep -c "Providers" src/app/layout.tsx && grep -c "var(--background)" src/app/layout.tsx | grep "^0$" && grep -c "var(--foreground)" src/app/layout.tsx | grep "^0$" && grep -c "var(--font-body)" src/app/layout.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/providers.tsx exists and contains `"use client"`
    - src/app/providers.tsx contains `import { ThemeProvider, UUIRouterProvider } from "@circos/ui"`
    - src/app/providers.tsx contains `export function Providers`
    - src/app/layout.tsx does NOT contain `"use client"`
    - src/app/layout.tsx contains `import { Providers } from "./providers"`
    - src/app/layout.tsx contains `<Providers>`
    - src/app/layout.tsx does NOT contain `var(--background)`
    - src/app/layout.tsx does NOT contain `var(--foreground)`
    - src/app/layout.tsx does NOT contain `var(--font-body)`
    - src/app/layout.tsx contains `bg-[var(--bg-primary)]`
    - src/app/layout.tsx contains `text-[var(--text-primary-900)]`
    - src/app/layout.tsx contains `font-[family-name:var(--font-text)]`
  </acceptance_criteria>
  <done>Root layout is a server component that wraps children in client Providers (ThemeProvider + UUIRouterProvider). Body uses UUI semantic tokens via Tailwind classes. Zero legacy var(--) tokens remain.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dashboard layout to UUI tokens</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <read_first>
    - src/app/(dashboard)/layout.tsx (current dashboard layout with var(--background) inline style)
  </read_first>
  <action>
In src/app/(dashboard)/layout.tsx:
- Remove the inline `style={{ backgroundColor: 'var(--background)' }}` from the outer `<div className="flex h-screen">`
- Replace with Tailwind class: change className to `"flex h-screen bg-[var(--bg-primary)]"`
- Keep everything else unchanged: the "use client" directive, all imports (usePathname, RealtimeProvider, DashboardSidebar, AgentFilterProvider, AgentListPanel), the isBoardDetailPage logic, the JSX structure
- The file should have ZERO inline style attributes after this change
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-c && grep -c "var(--background)" src/app/\(dashboard\)/layout.tsx | grep "^0$" && grep "bg-\[var(--bg-primary)\]" src/app/\(dashboard\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/layout.tsx does NOT contain `var(--background)`
    - src/app/(dashboard)/layout.tsx contains `bg-[var(--bg-primary)]`
    - src/app/(dashboard)/layout.tsx does NOT contain any `style={{` attributes
    - src/app/(dashboard)/layout.tsx still contains `"use client"` directive
    - src/app/(dashboard)/layout.tsx still contains `DashboardSidebar` import
  </acceptance_criteria>
  <done>Dashboard layout uses bg-[var(--bg-primary)] via Tailwind class. Zero legacy var(--) tokens. Zero inline style attributes.</done>
</task>

</tasks>

<verification>
- `grep -r "var(--background)\|var(--foreground)\|var(--font-body)" src/app/layout.tsx src/app/\(dashboard\)/layout.tsx` returns no matches
- `grep "ThemeProvider\|UUIRouterProvider" src/app/providers.tsx` returns 2 matches
- `grep "<Providers>" src/app/layout.tsx` returns 1 match
- `npx next build 2>&1 | tail -5` completes without errors (or at minimum, no new errors introduced)
</verification>

<success_criteria>
- providers.tsx exists with ThemeProvider + UUIRouterProvider wrapping children
- Root layout imports and uses Providers wrapper without "use client"
- Dashboard layout uses bg-[var(--bg-primary)] instead of inline var(--background)
- Zero legacy var(--) tokens in either layout file
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell/01-01-SUMMARY.md`
</output>
