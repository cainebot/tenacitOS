---
phase: 02-wave-c-boards
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/boards/page.tsx
autonomous: true
requirements: [WAVC-01]
must_haves:
  truths:
    - "Boards listing page renders with zero var(--*) inline styles"
    - "All layout uses Tailwind utility classes instead of inline style={{}}"
    - "Loading skeleton, error state, and board cards all use UUI tokens"
  artifacts:
    - path: "src/app/(dashboard)/boards/page.tsx"
      provides: "Boards listing page with UUI tokens"
      contains: "cx("
  key_links:
    - from: "src/app/(dashboard)/boards/page.tsx"
      to: "@openclaw/ui"
      via: "import cx"
      pattern: "from.*@openclaw/ui"
---

<objective>
Migrate the boards listing page (boards/page.tsx) from 100% inline style={{}} with var(--*) tokens to Tailwind UUI utility classes.

Purpose: WAVC-01 — the boards listing page is self-contained with no external component imports beyond next/link, making it a clean standalone migration.
Output: boards/page.tsx with zero var(--*) tokens and zero unnecessary inline style={{}} objects.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wave-b-pages/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate boards/page.tsx to UUI tokens</name>
  <files>src/app/(dashboard)/boards/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/boards/page.tsx
    - src/app/(dashboard)/skills/page.tsx (reference for proven migration pattern)
  </read_first>
  <action>
Replace all 16 inline style={{}} objects and 28 var(--*) references in boards/page.tsx with Tailwind UUI utility classes. Add `import { cx } from '@openclaw/ui'` for any conditional class merging.

Token mapping (proven in Phase 1):
- `fontFamily: 'var(--font-heading)'` → `font-heading`
- `fontFamily: 'var(--font-body)'` → `font-body`
- `color: 'var(--text-primary)'` → `text-primary`
- `color: 'var(--text-secondary)'` → `text-secondary`
- `color: 'var(--text-muted)'` → `text-muted`
- `background: 'var(--surface)'` → `bg-surface`
- `background: 'var(--surface-elevated)'` → `bg-surface-elevated`
- `border: '1px solid var(--border)'` → `border border-border`
- `background: 'var(--card)'` → `bg-card`
- `color: 'var(--accent)'` → `text-accent`
- `background: 'var(--background)'` → `bg-background`

Layout conversions:
- `display: 'flex'` → `flex`
- `alignItems: 'center'` → `items-center`
- `justifyContent: 'space-between'` → `justify-between`
- `gap: '16px'` → `gap-4`
- `marginBottom: '24px'` → `mb-6`
- `gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))'` → `grid grid-cols-[repeat(auto-fill,minmax(280px,1fr))]`
- `borderRadius: '8px'` → `rounded-lg`
- `borderRadius: '6px'` → `rounded-md`
- `padding: '20px'` → `p-5`
- `fontSize: '24px'` → `text-2xl`
- `fontSize: '14px'` → `text-sm`
- `fontSize: '13px'` → `text-xs`
- `fontWeight: 700` → `font-bold`
- `fontWeight: 600` → `font-semibold`
- `fontWeight: 500` → `font-medium`
- `animation: 'pulse 1.5s ease-in-out infinite'` → `animate-pulse`

Keep `maxWidth: '1200px'` as `max-w-[1200px]`.

Replace the inline `style={{}}` on every JSX element with `className="..."`. Use `cx()` from @openclaw/ui for any conditional classes (e.g., hover states).

Do NOT change any logic, state management, fetch calls, or JSX structure. Only swap styling approach.
  </action>
  <verify>
    <automated>grep -c "var(--" "src/app/(dashboard)/boards/page.tsx" | grep -q "^0$" && echo "PASS: zero var(-- tokens" || echo "FAIL: var(-- tokens remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/app/(dashboard)/boards/page.tsx` returns 0
    - `grep "cx(" src/app/(dashboard)/boards/page.tsx` finds at least 1 match
    - `grep "from.*@openclaw/ui" src/app/(dashboard)/boards/page.tsx` finds import
    - `grep -c "style={{" src/app/(dashboard)/boards/page.tsx` returns 0 or only non-token dynamic values (like dynamic width percentages)
    - File compiles with zero TypeScript errors
  </acceptance_criteria>
  <done>boards/page.tsx has zero var(--*) tokens, uses Tailwind UUI classes for all styling, imports cx from @openclaw/ui</done>
</task>

</tasks>

<verification>
- `grep -c "var(--" src/app/(dashboard)/boards/page.tsx` returns 0
- `grep "from.*@openclaw/ui" src/app/(dashboard)/boards/page.tsx` shows import
- No TypeScript errors in the file
</verification>

<success_criteria>
Boards listing page uses exclusively UUI tokens — zero var(--*) inline styles, all layout via Tailwind utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/02-wave-c-boards/02-01-SUMMARY.md`
</output>
