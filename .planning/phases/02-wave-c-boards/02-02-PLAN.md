---
phase: 02-wave-c-boards
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BoardKanban.tsx
  - src/components/BoardFilterBar.tsx
autonomous: true
requirements: [WAVC-02]
must_haves:
  truths:
    - "BoardKanban renders with zero var(--*) inline styles"
    - "BoardFilterBar renders with zero var(--*) inline styles"
    - "Both components use Tailwind UUI utility classes for all styling"
  artifacts:
    - path: "src/components/BoardKanban.tsx"
      provides: "Kanban board component with UUI tokens"
      contains: "cx("
    - path: "src/components/BoardFilterBar.tsx"
      provides: "Board filter bar with UUI tokens"
      contains: "cx("
  key_links:
    - from: "src/components/BoardKanban.tsx"
      to: "@circos/ui"
      via: "import cx"
      pattern: "from.*@circos/ui"
    - from: "src/components/BoardFilterBar.tsx"
      to: "@circos/ui"
      via: "import cx"
      pattern: "from.*@circos/ui"
---

<objective>
Migrate BoardKanban.tsx (5 var(--*), light) and BoardFilterBar.tsx (57 var(--*), heavy) from inline style={{}} with var(--*) tokens to Tailwind UUI utility classes.

Purpose: WAVC-02 sub-components — these are consumed by boards/[id]/page.tsx and must be migrated before the parent page.
Output: Both components with zero var(--*) tokens.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wave-b-pages/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate BoardKanban.tsx to UUI tokens</name>
  <files>src/components/BoardKanban.tsx</files>
  <read_first>
    - src/components/BoardKanban.tsx
    - src/app/(dashboard)/skills/page.tsx (reference for proven migration pattern)
  </read_first>
  <action>
Replace all 4 inline style={{}} objects and 5 var(--*) references in BoardKanban.tsx with Tailwind UUI utility classes. Add `import { cx } from '@circos/ui'` if conditional class merging is needed.

Token mapping (proven in Phase 1):
- `color: 'var(--text-primary)'` → `text-primary`
- `color: 'var(--text-secondary)'` → `text-secondary`
- `color: 'var(--text-muted)'` → `text-muted`
- `background: 'var(--surface)'` → `bg-surface`
- `background: 'var(--surface-elevated)'` → `bg-surface-elevated`
- `border: '1px solid var(--border)'` → `border border-border`
- `background: 'var(--card)'` → `bg-card`
- `background: 'var(--background)'` → `bg-background`

Layout conversions:
- `display: 'flex'` → `flex`
- `gap: '12px'` → `gap-3`
- `overflowX: 'auto'` → `overflow-x-auto`
- `width: '280px'` → `w-[280px]`
- `flexShrink: 0` → `shrink-0`
- `borderRadius: '6px'` → `rounded-md`
- `borderRadius: '8px'` → `rounded-lg`
- `padding` values → Tailwind p-* equivalents
- `fontSize` values → Tailwind text-* equivalents

Replace each inline `style={{}}` with `className="..."`. Do NOT change any drag-and-drop logic, event handlers, props, or JSX structure. Only swap styling approach.
  </action>
  <verify>
    <automated>grep -c "var(--" "src/components/BoardKanban.tsx" | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/components/BoardKanban.tsx` returns 0
    - `grep -c "style={{" src/components/BoardKanban.tsx` returns 0 or only non-token dynamic values
    - File compiles with zero TypeScript errors
  </acceptance_criteria>
  <done>BoardKanban.tsx has zero var(--*) tokens, uses Tailwind UUI classes for all styling</done>
</task>

<task type="auto">
  <name>Task 2: Migrate BoardFilterBar.tsx to UUI tokens</name>
  <files>src/components/BoardFilterBar.tsx</files>
  <read_first>
    - src/components/BoardFilterBar.tsx
    - src/app/(dashboard)/system/page.tsx (reference for heavy migration with many var(--*) tokens and cx() usage)
  </read_first>
  <action>
Replace all 30 inline style={{}} objects and 57 var(--*) references in BoardFilterBar.tsx with Tailwind UUI utility classes. Add `import { cx } from '@circos/ui'` for conditional class merging.

Token mapping (proven in Phase 1):
- `fontFamily: 'var(--font-heading)'` → `font-heading`
- `fontFamily: 'var(--font-body)'` → `font-body`
- `fontFamily: 'var(--font-mono)'` → `font-mono`
- `color: 'var(--text-primary)'` → `text-primary`
- `color: 'var(--text-secondary)'` → `text-secondary`
- `color: 'var(--text-muted)'` → `text-muted`
- `background: 'var(--surface)'` → `bg-surface`
- `background: 'var(--surface-elevated)'` → `bg-surface-elevated`
- `background: 'var(--surface-hover)'` → `hover:bg-surface-hover` (or `bg-surface-hover` if conditional)
- `border: '1px solid var(--border)'` → `border border-border`
- `background: 'var(--card)'` → `bg-card`
- `color: 'var(--accent)'` → `text-accent`
- `background: 'var(--accent)'` → `bg-accent`
- `color: 'var(--positive)'` / `'var(--success)'` → `text-success`
- `color: 'var(--warning)'` → `text-warning`
- `color: 'var(--negative)'` / `'var(--error)'` → `text-error`
- `background: 'var(--background)'` → `bg-background`
- `background: 'var(--surface-alt)'` → `bg-surface-alt`

Layout conversions follow same patterns as Task 1. Convert all `display`, `flexDirection`, `alignItems`, `justifyContent`, `gap`, `padding`, `margin`, `borderRadius`, `fontSize`, `fontWeight`, `width`, `height` to their Tailwind equivalents.

For hover/active states using inline style conditionals (e.g., ternary on isHovered), convert to Tailwind `hover:` modifiers where possible. For state-dependent styles (e.g., isActive ternary), use `cx()` conditional merging.

Do NOT change any filter logic, callback props, state management, or JSX structure. Only swap styling approach.
  </action>
  <verify>
    <automated>grep -c "var(--" "src/components/BoardFilterBar.tsx" | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/components/BoardFilterBar.tsx` returns 0
    - `grep "cx(" src/components/BoardFilterBar.tsx` finds at least 1 match
    - `grep "from.*@circos/ui" src/components/BoardFilterBar.tsx` finds import
    - `grep -c "style={{" src/components/BoardFilterBar.tsx` returns 0 or only non-token dynamic values
    - File compiles with zero TypeScript errors
  </acceptance_criteria>
  <done>BoardFilterBar.tsx has zero var(--*) tokens, uses Tailwind UUI classes with cx() for conditional styling</done>
</task>

</tasks>

<verification>
- `grep -c "var(--" src/components/BoardKanban.tsx` returns 0
- `grep -c "var(--" src/components/BoardFilterBar.tsx` returns 0
- Both files import from @circos/ui
- No TypeScript errors in either file
</verification>

<success_criteria>
BoardKanban and BoardFilterBar both use exclusively UUI tokens — zero var(--*) inline styles, all layout via Tailwind utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/02-wave-c-boards/02-02-SUMMARY.md`
</output>
