---
phase: 02-wave-c-boards
plan: "04"
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - src/app/(dashboard)/boards/[id]/page.tsx
autonomous: true
requirements: [WAVC-02]
must_haves:
  truths:
    - "Board detail page renders with zero var(--*) inline styles"
    - "Inline <style> block with @keyframes fadeIn and @keyframes pulse is removed"
    - "Animations use tailwindcss-animate classes instead of custom keyframes"
    - "ConfirmActionDialog import comes from @circos/ui, not @/components/ui/"
    - "Boards kanban and card detail panel function correctly after migration"
  artifacts:
    - path: "src/app/(dashboard)/boards/[id]/page.tsx"
      provides: "Board detail page with UUI tokens and tailwindcss-animate"
      contains: "cx("
  key_links:
    - from: "src/app/(dashboard)/boards/[id]/page.tsx"
      to: "@circos/ui"
      via: "import cx and ConfirmActionDialog"
      pattern: "from.*@circos/ui"
    - from: "src/app/(dashboard)/boards/[id]/page.tsx"
      to: "src/components/BoardKanban.tsx"
      via: "import BoardKanban"
      pattern: "from.*@/components/BoardKanban"
    - from: "src/app/(dashboard)/boards/[id]/page.tsx"
      to: "src/components/BoardFilterBar.tsx"
      via: "import BoardFilterBar"
      pattern: "from.*@/components/BoardFilterBar"
    - from: "src/app/(dashboard)/boards/[id]/page.tsx"
      to: "src/components/CardDetailPanel.tsx"
      via: "import CardDetailPanel"
      pattern: "from.*@/components/CardDetailPanel"
    - from: "src/app/(dashboard)/boards/[id]/page.tsx"
      to: "src/components/ColumnManager.tsx"
      via: "import ColumnManager"
      pattern: "from.*@/components/ColumnManager"
---

<objective>
Migrate the board detail page (boards/[id]/page.tsx, 921 lines) from inline style={{}} with var(--*) tokens to Tailwind UUI utility classes. Remove the inline `<style>` block with @keyframes and replace with tailwindcss-animate classes. Fix ConfirmActionDialog import path.

Purpose: WAVC-02 — this is the highest-complexity file in the entire migration. It imports all four sub-components migrated in plans 02-02 and 02-03, plus AgentSidePanel (migrated in Phase 3). Runs in Wave 2 after sub-components are done.
Output: boards/[id]/page.tsx with zero var(--*) tokens, no inline `<style>` block, tailwindcss-animate classes.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wave-b-pages/01-02-SUMMARY.md
@.planning/phases/02-wave-c-boards/02-02-SUMMARY.md
@.planning/phases/02-wave-c-boards/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate boards/[id]/page.tsx to UUI tokens and replace keyframes</name>
  <files>src/app/(dashboard)/boards/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/boards/[id]/page.tsx
    - src/app/(dashboard)/system/page.tsx (reference for heavy migration with 101 var(--*) tokens, cx() usage, and toast migration)
  </read_first>
  <action>
Replace all 41 inline style={{}} objects and 54 var(--*) references in boards/[id]/page.tsx with Tailwind UUI utility classes. This is the heaviest migration in the project (921 lines).

CRITICAL fixes:

1. Import path fix:
   - Change `import { ConfirmActionDialog } from '@/components/ui/confirm-action-dialog'` to import from `@circos/ui`
   - Add cx to the @circos/ui import: `import { cx, ConfirmActionDialog } from '@circos/ui'`

2. Remove inline `<style>` block (lines ~903-912):
   ```
   <style>{`
     @keyframes fadeIn {
       from { opacity: 0; transform: translateY(-4px); }
       to   { opacity: 1; transform: translateY(0); }
     }
     @keyframes pulse {
       0%, 100% { opacity: 0.5; }
       50% { opacity: 0.3; }
     }
   `}</style>
   ```
   Replace with tailwindcss-animate classes on the elements that reference these animations:
   - Elements using `animation: 'fadeIn ...'` → `className="animate-in fade-in slide-in-from-top-1"` (with appropriate duration)
   - Elements using `animation: 'pulse ...'` → `className="animate-pulse"` (Tailwind's built-in pulse)

3. Token mapping (proven in Phase 1):
   - `fontFamily: 'var(--font-heading)'` → `font-heading`
   - `fontFamily: 'var(--font-body)'` → `font-body`
   - `fontFamily: 'var(--font-mono)'` → `font-mono`
   - `color: 'var(--text-primary)'` → `text-primary`
   - `color: 'var(--text-secondary)'` → `text-secondary`
   - `color: 'var(--text-muted)'` → `text-muted`
   - `background: 'var(--surface)'` → `bg-surface`
   - `background: 'var(--surface-elevated)'` → `bg-surface-elevated`
   - `background: 'var(--surface-hover)'` → `hover:bg-surface-hover` or `bg-surface-hover`
   - `border: '1px solid var(--border)'` → `border border-border`
   - `background: 'var(--card)'` → `bg-card`
   - `color: 'var(--accent)'` → `text-accent`
   - `background: 'var(--accent)'` → `bg-accent`
   - `color: 'var(--positive)'` / `'var(--success)'` → `text-success`
   - `background: 'var(--positive)'` / `'var(--success)'` → `bg-success`
   - `color: 'var(--warning)'` → `text-warning`
   - `color: 'var(--negative)'` / `'var(--error)'` → `text-error`
   - `background: 'var(--negative)'` / `'var(--error)'` → `bg-error`
   - `background: 'var(--background)'` → `bg-background`
   - `background: 'var(--surface-alt)'` → `bg-surface-alt`

4. Layout conversions: Convert all `display`, `flexDirection`, `alignItems`, `justifyContent`, `gap`, `padding`, `margin`, `borderRadius`, `fontSize`, `fontWeight`, `width`, `height`, `minWidth`, `maxWidth`, `overflow`, `position`, `cursor`, `whiteSpace`, `textOverflow`, `zIndex` to Tailwind equivalents.

5. Board selector dropdown and scrum master dropdown are fully custom inline-styled — convert all their styling to Tailwind classes. Use `cx()` for open/closed state conditionals.

6. For state-dependent inline styles (ternaries on isOpen, isLoading, selectedCard, etc.), use `cx()` conditional class merging.

Do NOT change any board data logic, realtime subscriptions, kanban interactions, agent filter context, drag-and-drop, card CRUD operations, event handlers, or JSX structure. Only swap styling approach, fix the import path, and replace keyframes with tailwindcss-animate.

Do NOT migrate AgentSidePanel — that component is imported but will be migrated in Phase 3.
  </action>
  <verify>
    <automated>grep -c "var(--" "src/app/(dashboard)/boards/[id]/page.tsx" | grep -q "^0$" && echo "PASS: zero var(-- tokens" || echo "FAIL: var(-- tokens remain"; grep "@keyframes" "src/app/(dashboard)/boards/[id]/page.tsx" && echo "FAIL: @keyframes still present" || echo "PASS: @keyframes removed"; grep "@/components/ui/confirm-action-dialog" "src/app/(dashboard)/boards/[id]/page.tsx" && echo "FAIL: old import path" || echo "PASS: import path fixed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/app/(dashboard)/boards/[id]/page.tsx` returns 0
    - `grep "@keyframes" src/app/(dashboard)/boards/[id]/page.tsx` finds NO matches (inline style block removed)
    - `grep "<style>" src/app/(dashboard)/boards/[id]/page.tsx` finds NO matches
    - `grep "@/components/ui/confirm-action-dialog" src/app/(dashboard)/boards/[id]/page.tsx` finds NO matches (old path removed)
    - `grep "ConfirmActionDialog.*from.*@circos/ui" src/app/(dashboard)/boards/[id]/page.tsx` finds the updated import
    - `grep "cx(" src/app/(dashboard)/boards/[id]/page.tsx` finds at least 1 match
    - `grep "animate-in\|animate-pulse" src/app/(dashboard)/boards/[id]/page.tsx` finds tailwindcss-animate classes
    - File compiles with zero TypeScript errors
  </acceptance_criteria>
  <done>boards/[id]/page.tsx has zero var(--*) tokens, no inline style block, ConfirmActionDialog from @circos/ui, animations via tailwindcss-animate</done>
</task>

</tasks>

<verification>
- `grep -c "var(--" src/app/(dashboard)/boards/[id]/page.tsx` returns 0
- `grep "@keyframes" src/app/(dashboard)/boards/[id]/page.tsx` finds NO matches
- `grep "<style>" src/app/(dashboard)/boards/[id]/page.tsx` finds NO matches
- ConfirmActionDialog imports from @circos/ui
- cx() used for conditional class merging
- tailwindcss-animate classes present (animate-in, fade-in, animate-pulse)
- No TypeScript errors
</verification>

<success_criteria>
Board detail page and all its sub-components (migrated in plans 02-02 and 02-03) use exclusively UUI tokens — zero var(--*) inline styles, no inline keyframes, correct import paths, all layout via Tailwind utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/02-wave-c-boards/02-04-SUMMARY.md`
</output>
