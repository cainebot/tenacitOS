---
phase: 02-wave-c-boards
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CardDetailPanel.tsx
  - src/components/ColumnManager.tsx
autonomous: true
requirements: [WAVC-02]
must_haves:
  truths:
    - "CardDetailPanel renders with zero var(--*) inline styles"
    - "ColumnManager renders with zero var(--*) inline styles"
    - "ConfirmActionDialog import in CardDetailPanel comes from @circos/ui, not @/components/ui/"
  artifacts:
    - path: "src/components/CardDetailPanel.tsx"
      provides: "Card detail panel with UUI tokens"
      contains: "cx("
    - path: "src/components/ColumnManager.tsx"
      provides: "Column manager with UUI tokens"
      contains: "cx("
  key_links:
    - from: "src/components/CardDetailPanel.tsx"
      to: "@circos/ui"
      via: "import ConfirmActionDialog and cx"
      pattern: "from.*@circos/ui"
    - from: "src/components/ColumnManager.tsx"
      to: "@circos/ui"
      via: "import cx"
      pattern: "from.*@circos/ui"
---

<objective>
Migrate CardDetailPanel.tsx (45 var(--*), 33 style={{}}) and ColumnManager.tsx (60 var(--*), 40 style={{}}) from inline style={{}} with var(--*) tokens to Tailwind UUI utility classes. Update ConfirmActionDialog import path in CardDetailPanel.

Purpose: WAVC-02 sub-components — these are consumed by boards/[id]/page.tsx and must be migrated before the parent page.
Output: Both components with zero var(--*) tokens and correct import paths.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wave-b-pages/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CardDetailPanel.tsx to UUI tokens and fix import</name>
  <files>src/components/CardDetailPanel.tsx</files>
  <read_first>
    - src/components/CardDetailPanel.tsx
    - src/app/(dashboard)/system/page.tsx (reference for heavy migration with cx() conditional class ternaries)
  </read_first>
  <action>
Replace all 33 inline style={{}} objects and 45 var(--*) references in CardDetailPanel.tsx with Tailwind UUI utility classes. This is a heavy migration (807 lines).

CRITICAL import path fix:
- Change `import { ConfirmActionDialog } from '@/components/ui/confirm-action-dialog'` to `import { ConfirmActionDialog } from '@circos/ui'`

Add or update `import { cx } from '@circos/ui'` — merge with the ConfirmActionDialog import: `import { cx, ConfirmActionDialog } from '@circos/ui'`

Token mapping (proven in Phase 1):
- `fontFamily: 'var(--font-heading)'` → `font-heading`
- `fontFamily: 'var(--font-body)'` → `font-body`
- `fontFamily: 'var(--font-mono)'` → `font-mono`
- `color: 'var(--text-primary)'` → `text-primary`
- `color: 'var(--text-secondary)'` → `text-secondary`
- `color: 'var(--text-muted)'` → `text-muted`
- `background: 'var(--surface)'` → `bg-surface`
- `background: 'var(--surface-elevated)'` → `bg-surface-elevated`
- `background: 'var(--surface-hover)'` → `hover:bg-surface-hover` or `bg-surface-hover`
- `border: '1px solid var(--border)'` → `border border-border`
- `background: 'var(--card)'` → `bg-card`
- `color: 'var(--accent)'` → `text-accent`
- `background: 'var(--accent)'` → `bg-accent`
- `color: 'var(--positive)'` / `'var(--success)'` → `text-success`
- `background: 'var(--positive)'` / `'var(--success)'` → `bg-success`
- `color: 'var(--warning)'` → `text-warning`
- `color: 'var(--negative)'` / `'var(--error)'` → `text-error`
- `background: 'var(--negative)'` / `'var(--error)'` → `bg-error`
- `background: 'var(--background)'` → `bg-background`
- `background: 'var(--surface-alt)'` → `bg-surface-alt`

Layout conversions: Convert all `display`, `flexDirection`, `alignItems`, `justifyContent`, `gap`, `padding`, `margin`, `borderRadius`, `fontSize`, `fontWeight`, `width`, `height`, `overflow`, `position` to Tailwind equivalents.

For state-dependent inline styles (ternaries on isOpen, isEditing, etc.), use `cx()` conditional class merging.

Do NOT change any card detail logic, CRUD operations, form handling, event handlers, or JSX structure. Only swap styling approach and fix the import path.
  </action>
  <verify>
    <automated>grep -c "var(--" "src/components/CardDetailPanel.tsx" | grep -q "^0$" && echo "PASS: zero var(-- tokens" || echo "FAIL"; grep "from.*@/components/ui/confirm-action-dialog" "src/components/CardDetailPanel.tsx" && echo "FAIL: old import path" || echo "PASS: import path fixed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/components/CardDetailPanel.tsx` returns 0
    - `grep "@/components/ui/confirm-action-dialog" src/components/CardDetailPanel.tsx` finds NO matches (old path removed)
    - `grep "ConfirmActionDialog.*from.*@circos/ui" src/components/CardDetailPanel.tsx` finds the updated import
    - `grep "cx(" src/components/CardDetailPanel.tsx` finds at least 1 match
    - File compiles with zero TypeScript errors
  </acceptance_criteria>
  <done>CardDetailPanel.tsx has zero var(--*) tokens, ConfirmActionDialog imports from @circos/ui, uses cx() for conditional styling</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ColumnManager.tsx to UUI tokens</name>
  <files>src/components/ColumnManager.tsx</files>
  <read_first>
    - src/components/ColumnManager.tsx
    - src/app/(dashboard)/system/page.tsx (reference for heavy migration pattern)
  </read_first>
  <action>
Replace all 40 inline style={{}} objects and 60 var(--*) references in ColumnManager.tsx with Tailwind UUI utility classes. This is a heavy migration (890 lines).

Add `import { cx } from '@circos/ui'` for conditional class merging.

Token mapping (same as Task 1 — proven in Phase 1):
- `fontFamily: 'var(--font-heading)'` → `font-heading`
- `fontFamily: 'var(--font-body)'` → `font-body`
- `fontFamily: 'var(--font-mono)'` → `font-mono`
- `color: 'var(--text-primary)'` → `text-primary`
- `color: 'var(--text-secondary)'` → `text-secondary`
- `color: 'var(--text-muted)'` → `text-muted`
- `background: 'var(--surface)'` → `bg-surface`
- `background: 'var(--surface-elevated)'` → `bg-surface-elevated`
- `background: 'var(--surface-hover)'` → `hover:bg-surface-hover` or `bg-surface-hover`
- `border: '1px solid var(--border)'` → `border border-border`
- `background: 'var(--card)'` → `bg-card`
- `color: 'var(--accent)'` → `text-accent`
- `background: 'var(--accent)'` → `bg-accent`
- `color: 'var(--positive)'` / `'var(--success)'` → `text-success`
- `color: 'var(--warning)'` → `text-warning`
- `color: 'var(--negative)'` / `'var(--error)'` → `text-error`
- `background: 'var(--background)'` → `bg-background`
- `background: 'var(--surface-alt)'` → `bg-surface-alt`

Layout conversions: Convert all `display`, `flexDirection`, `alignItems`, `justifyContent`, `gap`, `padding`, `margin`, `borderRadius`, `fontSize`, `fontWeight`, `width`, `height`, `minWidth`, `maxWidth`, `overflow`, `position`, `cursor` to Tailwind equivalents.

For state-dependent inline styles (ternaries on isEditing, isDragging, etc.), use `cx()` conditional class merging.

Do NOT change any column management logic, drag-and-drop handling, CRUD operations, event handlers, or JSX structure. Only swap styling approach.
  </action>
  <verify>
    <automated>grep -c "var(--" "src/components/ColumnManager.tsx" | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--" src/components/ColumnManager.tsx` returns 0
    - `grep "cx(" src/components/ColumnManager.tsx` finds at least 1 match
    - `grep "from.*@circos/ui" src/components/ColumnManager.tsx` finds import
    - `grep -c "style={{" src/components/ColumnManager.tsx` returns 0 or only non-token dynamic values
    - File compiles with zero TypeScript errors
  </acceptance_criteria>
  <done>ColumnManager.tsx has zero var(--*) tokens, uses Tailwind UUI classes with cx() for conditional styling</done>
</task>

</tasks>

<verification>
- `grep -c "var(--" src/components/CardDetailPanel.tsx` returns 0
- `grep -c "var(--" src/components/ColumnManager.tsx` returns 0
- CardDetailPanel imports ConfirmActionDialog from @circos/ui (not @/components/ui/)
- Both files import cx from @circos/ui
- No TypeScript errors in either file
</verification>

<success_criteria>
CardDetailPanel and ColumnManager both use exclusively UUI tokens — zero var(--*) inline styles, correct import paths, all layout via Tailwind utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/02-wave-c-boards/02-03-SUMMARY.md`
</output>
