---
phase: 03-wave-c-agents-+-office
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/agents/[id]/page.tsx
  - src/components/organisms/AgentFormPanel.tsx
  - src/components/organisms/AgentSidePanel.tsx
autonomous: true
requirements: [WAVC-04]

must_haves:
  truths:
    - "Agent detail page renders with zero var(--*) inline styles"
    - "AgentFormPanel renders with zero var(--*) inline styles"
    - "AgentSidePanel renders with zero var(--*) inline styles"
    - "ConfirmActionDialog imports come from @circos/ui, not @/components/ui/"
    - "Hardcoded hex STATUS_COLORS, BADGE_STYLES, TASK_STATUS_COLORS replaced with UUI semantic tokens"
  artifacts:
    - path: "src/app/(dashboard)/agents/[id]/page.tsx"
      provides: "Agent detail page with UUI tokens"
      contains: "text-primary"
    - path: "src/components/organisms/AgentFormPanel.tsx"
      provides: "Agent form panel with UUI tokens"
      contains: "@circos/ui"
    - path: "src/components/organisms/AgentSidePanel.tsx"
      provides: "Agent side panel with UUI tokens"
      contains: "text-primary"
  key_links:
    - from: "src/components/organisms/AgentFormPanel.tsx"
      to: "@circos/ui"
      via: "ConfirmActionDialog import"
      pattern: "from '@circos/ui'"
    - from: "src/app/(dashboard)/agents/[id]/page.tsx"
      to: "src/components/organisms/AgentSidePanel.tsx"
      via: "component import"
      pattern: "import.*AgentSidePanel"
---

<objective>
Migrate agent detail page, AgentFormPanel, and AgentSidePanel from legacy var(--*) inline styles and hardcoded hex colors to UUI Tailwind tokens. Fix ConfirmActionDialog import path.

Purpose: Eliminate all legacy design system references from the agent detail surface and its sub-components.
Output: Three files with zero var(--*), corrected import paths, and semantic token usage.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate agents/[id]/page.tsx to UUI tokens</name>
  <files>src/app/(dashboard)/agents/[id]/page.tsx</files>
  <read_first>src/app/(dashboard)/agents/[id]/page.tsx</read_first>
  <action>
Migrate all 55 var(--*) inline styles and hardcoded hex colors to UUI Tailwind classes in agents/[id]/page.tsx (787 lines).

Standard token mapping (proven in Phase 1+2):
- `var(--text-primary)` -> `text-primary`, `var(--text-secondary)` -> `text-secondary`, `var(--text-muted)` -> `text-muted`
- `var(--surface)` -> `bg-surface`, `var(--surface-elevated)` -> `bg-surface-elevated`
- `var(--card)` -> `bg-card`, `var(--border)` -> `border-border` (with `border` width class)
- `var(--accent)` -> `bg-accent`/`text-accent`, `var(--background)` -> `bg-background`
- `var(--font-heading)` -> `font-heading`

Replace hardcoded hex color maps with Tailwind class maps:

STATUS_COLORS (line 35-42):
```
const STATUS_COLORS: Record<AgentStatus, string> = {
  idle: 'text-success',
  working: 'text-warning',
  error: 'text-error',
  offline: 'text-muted',
  thinking: 'text-info',
  queued: 'text-warning',
  executing_tool: 'text-warning',
}
```

BADGE_STYLES (line 45-49):
```
const BADGE_STYLES: Record<AgentBadge, { bg: string; color: string }> = {
  LEAD: { bg: 'bg-warning/10', color: 'text-warning' },
  SPC: { bg: 'bg-info/10', color: 'text-info' },
  INT: { bg: 'bg-success/10', color: 'text-success' },
}
```
Update consumers to use className instead of inline style for badge rendering.

TASK_STATUS_COLORS (line 51-56):
```
const TASK_STATUS_COLORS: Record<string, string> = {
  pending: 'bg-warning',
  claimed: 'bg-info',
  in_progress: 'bg-accent',
  completed: 'bg-success',
  failed: 'bg-error',
}
```
For the progress bar dot (line 729): use className with the bg-* token.
For the task status badge background+text (lines 737-738): use `${TASK_STATUS_COLORS[task.status] || 'bg-muted'}/10` for bg and the text variant for color.

For skill install status icons (lines 501-503 area): convert the object map from hex to Tailwind:
```
installed: { icon: CheckCircle, color: 'text-success', bg: 'bg-success/10' },
pending: { icon: Clock, color: 'text-warning', bg: 'bg-warning/10' },
failed: { icon: AlertCircle, color: 'text-error', bg: 'bg-error/10' },
```

Hardcoded `#ef4444` error displays (lines 310, 462, 536): replace with `text-error` className.

Dynamic department color (`agent.departments?.color ?? '#6366f1'`): keep as inline style (API-provided, per Phase 2 decision).

Convert all border inline styles to Tailwind border classes. Merge into existing className props.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-d && grep -c "var(--" src/app/\(dashboard\)/agents/\[id\]/page.tsx | grep -q "^0$" && echo "PASS: zero var(--*)" || echo "FAIL: var(--*) still present"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "var(--" src/app/(dashboard)/agents/[id]/page.tsx` returns 0
- `grep -c "#[0-9a-fA-F]\{6\}" src/app/(dashboard)/agents/[id]/page.tsx` returns at most 1 match (dynamic department color only)
- `grep "text-success\|text-warning\|text-error\|text-info\|bg-success\|bg-warning\|bg-error" src/app/(dashboard)/agents/[id]/page.tsx` returns multiple matches
  </acceptance_criteria>
  <done>agents/[id]/page.tsx has zero var(--*) inline styles, STATUS_COLORS/BADGE_STYLES/TASK_STATUS_COLORS use UUI semantic tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AgentFormPanel.tsx and AgentSidePanel.tsx to UUI tokens</name>
  <files>src/components/organisms/AgentFormPanel.tsx, src/components/organisms/AgentSidePanel.tsx</files>
  <read_first>src/components/organisms/AgentFormPanel.tsx, src/components/organisms/AgentSidePanel.tsx</read_first>
  <action>
**AgentFormPanel.tsx (1012 lines, 72 var(--*) occurrences):**

1. Fix ConfirmActionDialog import (line 5):
   FROM: `import { ConfirmActionDialog } from '@/components/ui/confirm-action-dialog'`
   TO: `import { ConfirmActionDialog } from '@circos/ui'`

2. Migrate all 72 var(--*) inline styles using standard token mapping:
   - `var(--text-primary)` -> `text-primary`, `var(--text-muted)` -> `text-muted`, `var(--text-secondary)` -> `text-secondary`
   - `var(--surface)` -> `bg-surface`, `var(--card)` -> `bg-card`, `var(--background)` -> `bg-background`
   - `var(--border)` -> `border-border`, `var(--accent)` -> `text-accent`/`bg-accent`
   - `var(--font-heading)` -> `font-heading`
   - `var(--success)` -> `text-success`, `var(--warning)` -> `text-warning`, `var(--error)` -> `text-error`

3. Replace hardcoded hex colors with UUI tokens where they map to semantic meaning.

4. Convert border inline styles to Tailwind: `border-b border-border`, `border-t border-border`, etc.

5. For form inputs with `style={{ backgroundColor: 'var(--background)', border: '1px solid var(--border)', color: 'var(--text-primary)' }}`:
   Replace with `className="bg-background border border-border text-primary"` and remove the style prop.

**AgentSidePanel.tsx (582 lines, 48 var(--*) occurrences):**

1. Migrate all 48 var(--*) inline styles using the same standard token mapping.

2. Convert all border, background, color inline styles to Tailwind classes.

3. For any hardcoded hex colors that map to semantic tokens, replace with UUI equivalents.

4. Check if AgentSidePanel imports ConfirmActionDialog -- if so, fix the import path to @circos/ui.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-d && FORM=$(grep -c "var(--" src/components/organisms/AgentFormPanel.tsx) && SIDE=$(grep -c "var(--" src/components/organisms/AgentSidePanel.tsx) && IMPORT=$(grep -c "@/components/ui/confirm-action-dialog" src/components/organisms/AgentFormPanel.tsx) && if [ "$FORM" = "0" ] && [ "$SIDE" = "0" ] && [ "$IMPORT" = "0" ]; then echo "PASS"; else echo "FAIL: FormPanel=$FORM SidePanel=$SIDE OldImport=$IMPORT"; fi</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "var(--" src/components/organisms/AgentFormPanel.tsx` returns 0
- `grep -c "var(--" src/components/organisms/AgentSidePanel.tsx` returns 0
- `grep "@/components/ui/confirm-action-dialog" src/components/organisms/AgentFormPanel.tsx` returns no matches
- `grep "@circos/ui" src/components/organisms/AgentFormPanel.tsx` returns a match
  </acceptance_criteria>
  <done>AgentFormPanel.tsx and AgentSidePanel.tsx have zero var(--*) inline styles, ConfirmActionDialog imported from @circos/ui</done>
</task>

</tasks>

<verification>
- `grep -rn "var(--" src/app/(dashboard)/agents/[id]/page.tsx src/components/organisms/AgentFormPanel.tsx src/components/organisms/AgentSidePanel.tsx` returns zero matches
- `grep "@/components/ui/confirm-action-dialog" src/components/organisms/AgentFormPanel.tsx` returns zero matches
- All three files use UUI semantic token classes
</verification>

<success_criteria>
Agent detail page, AgentFormPanel, and AgentSidePanel render with zero var(--*) inline styles. ConfirmActionDialog import comes from @circos/ui. All status/badge hex colors replaced with UUI semantic tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-c-agents-+-office/03-02-SUMMARY.md`
</output>
