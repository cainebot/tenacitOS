---
phase: 03-wave-c-agents-+-office
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/board-groups/page.tsx
  - src/app/(dashboard)/board-groups/[groupId]/page.tsx
  - src/app/(dashboard)/board-groups/[groupId]/edit/page.tsx
  - src/app/(dashboard)/board-groups/new/page.tsx
autonomous: true
requirements: [WAVC-05]

must_haves:
  truths:
    - "Board-groups listing page renders with zero var(--*) inline styles"
    - "Board-groups detail page renders with zero var(--*) inline styles"
    - "Board-groups edit page renders with zero var(--*) inline styles"
    - "Board-groups new page renders with zero var(--*) inline styles"
    - "ConfirmActionDialog import in board-groups/page.tsx comes from @circos/ui"
    - "Hardcoded #ff3b30 and #f97316 hex colors replaced with UUI semantic tokens"
  artifacts:
    - path: "src/app/(dashboard)/board-groups/page.tsx"
      provides: "Board groups listing with UUI tokens"
      contains: "text-primary"
    - path: "src/app/(dashboard)/board-groups/[groupId]/page.tsx"
      provides: "Board group detail with UUI tokens"
      contains: "text-primary"
    - path: "src/app/(dashboard)/board-groups/[groupId]/edit/page.tsx"
      provides: "Board group edit form with UUI tokens"
      contains: "text-primary"
    - path: "src/app/(dashboard)/board-groups/new/page.tsx"
      provides: "Board group new form with UUI tokens"
      contains: "text-primary"
  key_links:
    - from: "src/app/(dashboard)/board-groups/page.tsx"
      to: "@circos/ui"
      via: "ConfirmActionDialog import"
      pattern: "from '@circos/ui'"
---

<objective>
Migrate all four board-groups pages from legacy var(--*) inline styles and hardcoded hex colors to UUI Tailwind tokens. Fix ConfirmActionDialog import.

Purpose: Eliminate all legacy design system references from the board-groups surface.
Output: Four files with zero var(--*) and corrected imports.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate board-groups/page.tsx and board-groups/[groupId]/page.tsx</name>
  <files>src/app/(dashboard)/board-groups/page.tsx, src/app/(dashboard)/board-groups/[groupId]/page.tsx</files>
  <read_first>src/app/(dashboard)/board-groups/page.tsx, src/app/(dashboard)/board-groups/[groupId]/page.tsx</read_first>
  <action>
**board-groups/page.tsx (229 lines):**

1. Fix ConfirmActionDialog import (line 7):
   FROM: `import { ConfirmActionDialog } from '@/components/ui/confirm-action-dialog'`
   TO: `import { ConfirmActionDialog } from '@circos/ui'`

2. Migrate all var(--*) inline styles using standard token mapping:
   - `var(--text-primary)` -> `text-primary`, `var(--text-muted)` -> `text-muted`
   - `var(--surface)` -> `bg-surface`, `var(--card)` -> `bg-card`
   - `var(--border)` -> `border-border` (with border width class)
   - `var(--accent)` -> `bg-accent`, `color: '#fff'` -> `text-white`
   - `var(--font-heading)` -> `font-heading`

3. Replace hardcoded error styling (line 97):
   `style={{ backgroundColor: '#ff3b3015', border: '1px solid #ff3b3040', color: '#ff3b30' }}`
   -> `className="bg-error/10 border border-error/25 text-error"`

4. Convert all border inline styles to Tailwind border classes:
   - `borderBottom: '1px solid var(--border)'` -> `border-b border-border`
   - `borderTop: '1px solid var(--border)'` -> `border-t border-border`
   - `border: '1px solid var(--border)'` -> `border border-border`
   - `borderColor: 'var(--border)'` -> `divide-border` (for divide-y containers)

5. Merge all migrated styles into existing className props.

**board-groups/[groupId]/page.tsx (153 lines):**

1. Migrate all var(--*) inline styles using same token mapping.

2. Replace hardcoded error styling (line 77):
   `style={{ backgroundColor: '#ff3b3015', border: '1px solid #ff3b3040', color: '#ff3b30' }}`
   -> `className="bg-error/10 border border-error/25 text-error"`

3. Convert all border and background inline styles to Tailwind classes.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-d && LIST=$(grep -c "var(--" src/app/\(dashboard\)/board-groups/page.tsx) && DETAIL=$(grep -c "var(--" src/app/\(dashboard\)/board-groups/\[groupId\]/page.tsx) && IMPORT=$(grep -c "@/components/ui/confirm-action-dialog" src/app/\(dashboard\)/board-groups/page.tsx) && if [ "$LIST" = "0" ] && [ "$DETAIL" = "0" ] && [ "$IMPORT" = "0" ]; then echo "PASS"; else echo "FAIL: list=$LIST detail=$DETAIL import=$IMPORT"; fi</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "var(--" src/app/(dashboard)/board-groups/page.tsx` returns 0
- `grep -c "var(--" src/app/(dashboard)/board-groups/[groupId]/page.tsx` returns 0
- `grep -c "#ff3b30" src/app/(dashboard)/board-groups/page.tsx` returns 0
- `grep -c "#ff3b30" src/app/(dashboard)/board-groups/[groupId]/page.tsx` returns 0
- `grep "@/components/ui/confirm-action-dialog" src/app/(dashboard)/board-groups/page.tsx` returns no matches
- `grep "@circos/ui" src/app/(dashboard)/board-groups/page.tsx` returns a match
  </acceptance_criteria>
  <done>board-groups listing and detail pages have zero var(--*) and zero hardcoded hex colors, ConfirmActionDialog from @circos/ui</done>
</task>

<task type="auto">
  <name>Task 2: Migrate board-groups edit and new pages</name>
  <files>src/app/(dashboard)/board-groups/[groupId]/edit/page.tsx, src/app/(dashboard)/board-groups/new/page.tsx</files>
  <read_first>src/app/(dashboard)/board-groups/[groupId]/edit/page.tsx, src/app/(dashboard)/board-groups/new/page.tsx</read_first>
  <action>
**board-groups/[groupId]/edit/page.tsx (240 lines):**

1. Migrate all var(--*) inline styles using standard token mapping.

2. Replace hardcoded #f97316 "currently in another group" badge (line 196):
   `style={{ backgroundColor: '#f9731620', color: '#f97316', border: '1px solid #f9731640' }}`
   -> `className="bg-warning/10 text-warning border border-warning/25"`

3. Form input pattern: replace `style={{ backgroundColor: 'var(--background)', border: '1px solid var(--border)', color: 'var(--text-primary)', outline: 'none' }}` with `className="bg-background border border-border text-primary outline-none"`.

4. Convert all border, background, color inline styles to Tailwind classes.

5. Button styles: `style={{ backgroundColor: 'var(--accent)', color: '#fff' }}` -> `className="bg-accent text-white"`. Cancel button: `style={{ color: 'var(--text-muted)', backgroundColor: 'transparent', border: '1px solid var(--border)' }}` -> `className="text-muted bg-transparent border border-border"`.

**board-groups/new/page.tsx (223 lines):**

1. Same migration pattern as edit page (nearly identical form structure).

2. Migrate all var(--*) inline styles using standard token mapping.

3. Form inputs: same bg-background border border-border text-primary pattern.

4. Buttons: same accent/cancel pattern as edit page.

5. Convert all remaining inline style props to Tailwind classes.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-d && EDIT=$(grep -c "var(--" src/app/\(dashboard\)/board-groups/\[groupId\]/edit/page.tsx) && NEW=$(grep -c "var(--" src/app/\(dashboard\)/board-groups/new/page.tsx) && if [ "$EDIT" = "0" ] && [ "$NEW" = "0" ]; then echo "PASS"; else echo "FAIL: edit=$EDIT new=$NEW"; fi</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "var(--" src/app/(dashboard)/board-groups/[groupId]/edit/page.tsx` returns 0
- `grep -c "var(--" src/app/(dashboard)/board-groups/new/page.tsx` returns 0
- `grep -c "#f97316" src/app/(dashboard)/board-groups/[groupId]/edit/page.tsx` returns 0
- `grep "bg-background\|border-border\|text-primary\|bg-accent" src/app/(dashboard)/board-groups/[groupId]/edit/page.tsx` returns multiple matches
  </acceptance_criteria>
  <done>Board-groups edit and new pages have zero var(--*) inline styles, hardcoded hex colors replaced with UUI semantic tokens</done>
</task>

</tasks>

<verification>
- `grep -rn "var(--" src/app/(dashboard)/board-groups/` returns zero matches across all four files
- `grep -rn "#ff3b30\|#f97316" src/app/(dashboard)/board-groups/` returns zero matches
- ConfirmActionDialog imported from @circos/ui in board-groups/page.tsx
</verification>

<success_criteria>
All four board-groups pages render with zero var(--*) inline styles, zero hardcoded hex colors, and ConfirmActionDialog imported from @circos/ui.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-c-agents-+-office/03-03-SUMMARY.md`
</output>
