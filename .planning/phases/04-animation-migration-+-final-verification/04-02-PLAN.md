---
phase: 04-animation-migration-+-final-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SkillPreviewCard.tsx
  - src/components/DiscoveryPanel.tsx
autonomous: true
requirements:
  - VERF-01

must_haves:
  truths:
    - "SkillPreviewCard contains zero var(--*) inline style references"
    - "DiscoveryPanel contains zero var(--*) inline style references"
    - "Both components use Tailwind UUI token classes for all colors, fonts, borders"
    - "JS hover handlers replaced with Tailwind hover: modifiers where possible"
  artifacts:
    - path: "src/components/SkillPreviewCard.tsx"
      provides: "Fully migrated SkillPreviewCard with UUI tokens"
    - path: "src/components/DiscoveryPanel.tsx"
      provides: "Fully migrated DiscoveryPanel with UUI tokens"
  key_links:
    - from: "src/components/SkillPreviewCard.tsx"
      to: "src/components/SmartAddModal.tsx"
      via: "import and render"
      pattern: "SkillPreviewCard"
    - from: "src/components/DiscoveryPanel.tsx"
      to: "src/components/SmartAddModal.tsx"
      via: "import and render"
      pattern: "DiscoveryPanel"
---

<objective>
Migrate all var(--*) inline styles in SkillPreviewCard.tsx and DiscoveryPanel.tsx to Tailwind UUI token classes, and replace JS hover handlers with Tailwind hover: modifiers.

Purpose: These two components are imported by SmartAddModal and must be fully migrated to UUI tokens for the VERF-01 zero-legacy-tokens sweep to pass.
Output: Both files with zero var(--*) inline styles.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/SkillPreviewCard.tsx
@src/components/DiscoveryPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SkillPreviewCard.tsx var(--*) to Tailwind tokens</name>
  <files>src/components/SkillPreviewCard.tsx</files>
  <read_first>src/components/SkillPreviewCard.tsx</read_first>
  <action>
Replace all var(--*) inline styles in SkillPreviewCard.tsx with Tailwind classes. The file has ~17 var(--*) references.

**Token mappings (same as established in Phases 1-3):**
- `fontFamily: 'var(--font-heading)'` -> `font-heading` (or `font-sans` if no heading token)
- `fontFamily: 'var(--font-body)'` -> `font-sans`
- `fontFamily: 'var(--font-mono)'` -> `font-mono`
- `color: 'var(--text-primary)'` -> `text-foreground`
- `color: 'var(--text-secondary)'` -> `text-muted-foreground`
- `color: 'var(--accent)'` -> `text-accent`
- `backgroundColor: 'var(--surface-elevated)'` -> `bg-surface-elevated`
- `backgroundColor: 'var(--surface)'` -> `bg-surface` or `bg-background`
- `border: '1px solid var(--border)'` -> `border border-border`
- `borderColor: 'var(--border)'` -> `border-border`

**Specific changes:**

1. Card container div (line 74-83): Convert inline style to className. `backgroundColor: 'var(--surface-elevated)'` -> `bg-surface-elevated`, `border: '1px solid rgba(255,255,255,0.06)'` -> `border border-white/[0.06]`, layout props -> Tailwind utilities (`rounded-xl p-5 flex flex-col gap-3`).

2. Title input style (lines 45-56): The titleStyle and descStyle objects use var(--*) for font, color, backgroundColor. Convert to className-based approach:
   - Remove the titleStyle/descStyle CSSProperties objects
   - Apply Tailwind classes directly: `className={cx('border-none outline-none bg-transparent w-full transition-colors font-heading text-lg font-bold text-foreground px-1.5 py-0.5 rounded-md -mx-1.5', titleFocus ? 'bg-surface border border-border' : titleHover ? 'bg-surface' : 'border border-transparent')}`
   - Same pattern for description textarea

3. Emoji picker button (lines 92-108): Replace onMouseEnter/onMouseLeave with `hover:bg-surface` class.

4. Emoji grid buttons (lines 114-129): Replace onMouseEnter/onMouseLeave with `hover:bg-surface-elevated` class. Use cx() for selected state.

5. Source URL link (lines 162-174): `color: 'var(--accent)'` -> `text-accent`, `fontFamily: 'var(--font-mono)'` -> `font-mono`.

6. Content textarea (lines 179-201): Convert inline styles to classes. `fontFamily: 'var(--font-mono)'` -> `font-mono`, `backgroundColor: 'var(--surface)'` -> `bg-surface`. Replace onFocus/onBlur border handlers with `focus:border-border` class.

7. Keep hardcoded `#8B8B8B` color for content textarea text (line 185) — not a var(--*) token, can convert to `text-[#8B8B8B]` Tailwind arbitrary value.

Import cx from @circos/ui if not already present (already imported on line 4).
  </action>
  <verify>
    <automated>grep -c "var(--" src/components/SkillPreviewCard.tsx | grep -q "^0$" && echo "PASS" || echo "FAIL: var(-- refs remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep "var(--" src/components/SkillPreviewCard.tsx returns 0 matches
    - Component still exports SkillPreviewCard (named + default)
  </acceptance_criteria>
  <done>SkillPreviewCard has zero var(--*) inline styles, all colors/fonts/borders use Tailwind UUI tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate DiscoveryPanel.tsx var(--*) to Tailwind tokens</name>
  <files>src/components/DiscoveryPanel.tsx</files>
  <read_first>src/components/DiscoveryPanel.tsx</read_first>
  <action>
Replace all var(--*) inline styles in DiscoveryPanel.tsx with Tailwind classes. The file has ~22 var(--*) references.

**Specific changes:**

1. Search input (lines 60-77): Convert entire inline style to className:
   `className="w-full px-3 py-2 rounded-lg border border-border bg-surface-elevated text-foreground text-sm font-sans outline-none box-border"`

2. Loading text (lines 80-88): Convert to className:
   `className="text-xs text-muted-foreground font-sans m-0"`

3. Empty state text (lines 92-100): Same pattern as loading text.

4. Result card divs (lines 107-125):
   - Container: `className="px-3 py-2.5 rounded-lg border border-border bg-surface-elevated cursor-pointer flex flex-col gap-1 hover:border-accent transition-colors"`
   - Remove onMouseEnter/onMouseLeave handlers — use `hover:border-accent` class

5. Skill name span (lines 128-133): `className="text-sm font-semibold text-foreground font-sans"`

6. Source badge span (lines 136-158): Convert the conditional backgroundColor/color inline styles to Tailwind classes using cx():
   - skills_sh: `bg-accent text-white`
   - clawhub: `bg-[#6366f1] text-white` (keep hex as arbitrary value since it's a brand color)
   - default: `bg-border text-muted-foreground`
   - Common: `text-[10px] px-1.5 py-0.5 rounded font-sans whitespace-nowrap`

7. Summary span (lines 160-165): `className="text-xs text-muted-foreground font-sans"`

8. Version span (lines 167-172): `className="text-[11px] text-muted-foreground font-sans"`
  </action>
  <verify>
    <automated>grep -c "var(--" src/components/DiscoveryPanel.tsx | grep -q "^0$" && echo "PASS" || echo "FAIL: var(-- refs remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep "var(--" src/components/DiscoveryPanel.tsx returns 0 matches
    - Component still exports DiscoveryPanel as named export
  </acceptance_criteria>
  <done>DiscoveryPanel has zero var(--*) inline styles, all colors/fonts/borders use Tailwind UUI tokens, hover handlers replaced with Tailwind hover: modifiers</done>
</task>

</tasks>

<verification>
- `grep -c "var(--" src/components/SkillPreviewCard.tsx` returns 0
- `grep -c "var(--" src/components/DiscoveryPanel.tsx` returns 0
</verification>

<success_criteria>
Both SkillPreviewCard.tsx and DiscoveryPanel.tsx contain zero var(--*) inline styles. All colors, fonts, and borders use Tailwind UUI tokens. JS hover handlers replaced with Tailwind hover: modifiers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-animation-migration-+-final-verification/04-02-SUMMARY.md`
</output>
