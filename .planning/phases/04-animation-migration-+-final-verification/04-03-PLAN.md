---
phase: 04-animation-migration-+-final-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: true
requirements:
  - VERF-01
  - VERF-02
  - VERF-03
  - VERF-04

must_haves:
  truths:
    - "grep -r 'var(--' across all Wave B+C files returns zero matches (excluding Recharts, Office2D, Monaco allowlist)"
    - "next build completes with zero errors"
    - "Boards kanban page loads and renders without console errors"
    - "Agent CRUD page loads and renders without console errors"
  artifacts: []
  key_links: []
---

<objective>
Run a comprehensive verification sweep: grep all Wave B+C files for remaining var(--*) tokens, fix any stragglers, run `next build`, and verify functional correctness of boards and agents.

Purpose: Final gate before the milestone can close -- confirms zero legacy tokens remain and the app builds and functions correctly (VERF-01 through VERF-04).
Output: Clean grep results, successful build, functional verification.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep sweep for remaining var(--*) tokens and fix stragglers</name>
  <files></files>
  <read_first>.planning/ROADMAP.md</read_first>
  <action>
Run a comprehensive grep sweep across ALL Wave B+C files for remaining var(--*) references.

**Wave B+C file paths to scan:**
- src/app/(app)/skills/page.tsx
- src/app/(app)/skills/[id]/page.tsx
- src/app/(app)/costs/page.tsx
- src/app/(app)/system/page.tsx
- src/app/(app)/analytics/page.tsx
- src/app/(app)/boards/page.tsx
- src/app/(app)/boards/[id]/page.tsx
- src/app/(app)/agents/page.tsx
- src/app/(app)/agents/[id]/page.tsx
- src/app/(app)/board-groups/page.tsx
- src/app/(app)/board-groups/[id]/page.tsx
- src/app/(app)/board-groups/[id]/edit/page.tsx
- src/app/(app)/board-groups/new/page.tsx
- src/app/(app)/office/page.tsx
- src/components/SmartAddModal.tsx
- src/components/SkillPreviewCard.tsx
- src/components/DiscoveryPanel.tsx
- Any board sub-components (BoardKanban, BoardFilterBar, CardDetailPanel, ColumnManager)
- Any agent sub-components (AgentOrganigrama, AgentFormPanel, AgentSidePanel)

**Command:**
```
grep -rn "var(--" src/app/\(app\)/skills/ src/app/\(app\)/costs/ src/app/\(app\)/system/ src/app/\(app\)/analytics/ src/app/\(app\)/boards/ src/app/\(app\)/agents/ src/app/\(app\)/board-groups/ src/app/\(app\)/office/ src/components/SmartAddModal.tsx src/components/SkillPreviewCard.tsx src/components/DiscoveryPanel.tsx src/components/BoardKanban.tsx src/components/BoardFilterBar.tsx src/components/CardDetailPanel.tsx src/components/ColumnManager.tsx src/components/AgentOrganigrama.tsx src/components/AgentFormPanel.tsx src/components/AgentSidePanel.tsx 2>/dev/null
```

**Allowlist (ignore these matches):**
- Recharts component internals (inside `<ResponsiveContainer>`, `<BarChart>`, etc.)
- Office2D Phaser canvas code
- Monaco editor internals
- Any file in src/components/charts/ or similar chart directories

**If any non-allowlisted var(--*) matches found:**
- Fix each one using the established token mappings from Phases 1-3
- Common mappings: var(--text-primary) -> text-foreground, var(--surface-elevated) -> bg-surface-elevated, var(--border) -> border-border, var(--font-body) -> font-sans, var(--accent) -> text-accent/bg-accent, var(--negative) -> text-destructive

**If zero non-allowlisted matches found:** Report clean sweep.
  </action>
  <verify>
    <automated>grep -rn "var(--" src/app/\(app\)/skills/ src/app/\(app\)/costs/ src/app/\(app\)/system/ src/app/\(app\)/analytics/ src/app/\(app\)/boards/ src/app/\(app\)/agents/ src/app/\(app\)/board-groups/ src/app/\(app\)/office/ src/components/SmartAddModal.tsx src/components/SkillPreviewCard.tsx src/components/DiscoveryPanel.tsx 2>/dev/null | grep -v "Recharts\|recharts\|Office2D\|monaco\|chart" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The grep sweep returns 0 non-allowlisted var(--*) matches across all Wave B+C files
  </acceptance_criteria>
  <done>Zero legacy var(--*) tokens remain in any Wave B+C file (excluding allowlisted chart/canvas internals)</done>
</task>

<task type="auto">
  <name>Task 2: Run next build and verify functional correctness</name>
  <files></files>
  <read_first></read_first>
  <action>
1. Run `npx next build` (or `npm run build` / `pnpm build` depending on package manager) and verify it completes with zero TypeScript errors and zero build errors.

2. If build fails:
   - Read the error output carefully
   - Fix any TypeScript errors (likely import issues or type mismatches from the migration)
   - Re-run build until it passes

3. After build passes, verify functional correctness by checking that the key pages can be statically analyzed / built without errors:
   - boards pages (VERF-03: kanban drag-and-drop correctness)
   - agents pages (VERF-04: agent CRUD correctness)
   - Note: Full interactive testing requires a running dev server and human verification, but build success confirms no TypeScript/import/rendering errors

4. Check for any motion/react import remaining anywhere in Wave B+C files:
   ```
   grep -rn "from 'motion" src/components/SmartAddModal.tsx src/components/SkillPreviewCard.tsx src/components/DiscoveryPanel.tsx 2>/dev/null
   ```
   Should return zero matches.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run build (or equivalent) exits with code 0
    - No TypeScript errors in build output
    - grep "from 'motion" across SmartAddModal, SkillPreviewCard, DiscoveryPanel returns 0 matches
  </acceptance_criteria>
  <done>Build succeeds with zero errors, no motion imports remain, all Wave B+C pages compile correctly</done>
</task>

</tasks>

<verification>
- `grep -rn "var(--"` across all Wave B+C files returns 0 non-allowlisted matches (VERF-01)
- `npm run build` exits 0 (VERF-02)
- boards pages build without error (VERF-03 build-level check)
- agents pages build without error (VERF-04 build-level check)
</verification>

<success_criteria>
Zero legacy var(--*) tokens across all Wave B+C files (VERF-01). Build succeeds (VERF-02). Boards and agents pages compile and build without errors (VERF-03, VERF-04 at build level).
</success_criteria>

<output>
After completion, create `.planning/phases/04-animation-migration-+-final-verification/04-03-SUMMARY.md`
</output>
