export default function ActivityPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">Activity — rebuilding with UUI</p>
    </div>
  );
}
