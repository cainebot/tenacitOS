export default function WorkflowsPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">Workflows — rebuilding with UUI</p>
    </div>
  );
}
