export default function SystemPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">System — rebuilding with UUI</p>
    </div>
  );
}
