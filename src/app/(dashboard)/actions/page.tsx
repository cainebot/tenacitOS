export default function ActionsPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">Actions — rebuilding with UUI</p>
    </div>
  );
}
