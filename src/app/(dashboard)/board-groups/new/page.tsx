export default function NewBoardGroupPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">New Board Group — rebuilding with UUI</p>
    </div>
  );
}
