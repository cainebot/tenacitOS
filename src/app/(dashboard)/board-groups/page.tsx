export default function BoardGroupsPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">Board Groups — rebuilding with UUI</p>
    </div>
  );
}
