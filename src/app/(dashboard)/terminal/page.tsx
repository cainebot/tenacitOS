export default function TerminalPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">Terminal — rebuilding with UUI</p>
    </div>
  );
}
