export default function BoardDetailPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-sm text-[var(--text-tertiary-600)]">Board Detail — rebuilding with UUI</p>
    </div>
  );
}
