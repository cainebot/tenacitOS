import { describe, it } from 'vitest'

describe('POST /api/messages/[messageId]/abort (Phase 102)', () => {
  it.todo('should set abort_requested=true on the message row')
  it.todo('should return 200 with { ok: true } on success')
  it.todo('should return 500 with error message on Supabase failure')
})
